# 第 05 章 · 多盘管理与规划

> **一句话本质**：多盘服务器的核心是三件事——**每块盘干什么（分工）、怎么接进目录树（挂载/fstab）、坏盘了数据还在不在（冗余 RAID/LVM）**。硬盘不会自动变成目录，Linux 靠"挂载"把某个文件系统接到某个路径。

## 📍 这一章在哪
- 前情：第 03 章知道 `/mnt`/`/data` 是"数据地盘的挂载点"。
- 本章：把多块物理盘的规划、挂载、冗余讲透。
- 贯穿：回扣第 03 章"大文件放数据盘"的原则。

---

## 1. 存储的分层链路

从物理硬盘到你 `cd` 进去的目录，中间有好几层：

```
物理硬盘
  → 块设备 /dev/nvme0n1            （操作系统眼里的一个"盘"）
    → 分区 /dev/nvme0n1p1，可选
      → 文件系统 ext4 / xfs        （格式化后才有）
        → 挂载点 /mnt/data1        （接到目录树的某个位置）
          → 你看到的目录和文件
```

- `nvme` = NVMe SSD（高速固态盘），延迟低、吞吐高，适合数据读取、缓存、checkpoint。
- 块设备只是"一个盘"，**还不是你能 `cd` 进去的目录**——要先有文件系统、再挂载。

---

## 2. 为什么需要"挂载"

Linux 不像 Windows 天然显示 `C:`、`D:`。它只有**一棵目录树**，从 `/` 开始。硬盘插上也只是个块设备 `/dev/nvme0n1`，本身进不去。

> **"挂载"= 把某块盘上的文件系统，接到目录树的某个位置。**

```bash
mount /dev/nvme0n1p1 /mnt/data1   # 把这个分区的文件系统显示到 /mnt/data1 下
```

- 挂载前：`/mnt/data1` 只是根盘上一个普通空目录。
- 挂载后：`/mnt/data1` 变成那块盘的入口。

> 挂的是**分区**（`nvme0n1p1`）还是**整盘**（`nvme0n1`）取决于这块盘怎么做的：常规做法是先分区再格式化，挂分区；数据盘也有"整盘不分区、直接做一个文件系统"的做法，那就直接挂 `nvme0n1`。`lsblk -f` 一看便知。

> ⚠️ **最常见的坑**：挂载**没成功**就往 `/mnt/data1` 写东西 → 其实写进了**根盘 `/`**，悄悄把系统盘塞满。下数据集、存 checkpoint 最容易踩。**写之前先确认挂载成功。**

---

## 3. 挂载 vs 格式化（务必分清）

```
mount   挂载：读取已有文件系统，不破坏数据
mkfs    格式化：创建新文件系统，会清掉原有数据 ☠
fdisk   分区：改磁盘结构，也可能破坏数据 ☠
```

> **共享服务器纪律**：看到一块"裸盘"**绝不能直接 `mkfs`**。先确认三件事：这盘是否真没人用、是否允许初始化、要不要组 RAID/LVM。归属与决策通常在管理员手里——**先问，再动**。

---

## 4. 多盘分工（典型规划）

```
/                系统盘：操作系统 + 基础软件，不塞大文件
/home/alice      用户目录：代码、配置、小文件
/mnt/data1 /data 数据盘：数据集、模型、checkpoint（大文件主战场）
/dev/shm         内存盘：极快、重启丢失，临时缓存
```

推荐在数据盘下按用途分目录：

```
/data/alice/datasets      数据集
/data/alice/models        预训练模型、缓存
/data/alice/checkpoints   训练输出
/data/alice/envs          conda 环境
/data/alice/cache         pip/缓存
```

> 核心原则（回扣第 03 章）：**根盘只放系统和少量工具，大体积内容放挂载盘。**

---

## 5. /etc/fstab：开机自动挂载

手动 `mount` 重启就没了。要开机自动挂载，写进 `/etc/fstab`（一行一块盘）：

```bash
blkid                              # 先拿到盘的 UUID
# /etc/fstab 里加一行（六列：设备 挂载点 文件系统 选项 dump fsck顺序）：
# UUID=<盘的真实UUID>  /mnt/data1  ext4  defaults  0  2
```

- 用 **UUID** 而不是 `/dev/nvme0n1p1`：设备名可能因插槽/枚举顺序变化而变，UUID 永远跟着盘走。
- ⚠️ fstab 写错可能**导致开不了机**——生产机改它要格外谨慎，通常归管理员。

---

## 6. RAID / LVM（冗余与灵活）

| 概念 | 一句话 | 取舍 |
|---|---|---|
| **RAID0** | 多盘拼一块、并行加速 | 快，但**坏一块全丢** |
| **RAID1** | 镜像，两块存一样 | 安全，**容量减半** |
| **RAID10** | 兼顾速度与冗余 | 成本高 |
| **LVM** | 逻辑卷，灵活扩容/合并多盘 | 管理灵活，非加速也非冗余 |

> 深度学习数据盘的常见取舍：临时可重建的数据可用 RAID0 求速度；重要且难重建的（标注、产出）要冗余（RAID1/10）或勤备份。**别把"快"和"安全"混为一谈。**

---

## 7. 必会的查盘命令

```bash
lsblk -f          # 有哪些盘、有无文件系统、挂到哪里   [只读，首选]
df -hT            # 已挂载文件系统的容量和使用率        [只读]
df -hT /mnt/data1 # 确认某路径背后是哪块盘             [只读]
findmnt           # 看当前挂载关系                    [只读]
blkid             # 看 UUID（写 fstab 用）            [只读]
du -sh *          # 看目录各占多少空间                [只读]
```

> **黄金判断**：`df -hT /mnt/data1` 若显示**数据盘设备**（如 `/dev/nvme0n1p1`）、容量符合 → 正在用数据盘；若显示的是**根盘设备**（如 `/dev/nvme0n1p2`、`/dev/sda2` 或用了 LVM 的 `/dev/mapper/...`）→ **没挂载成功**，此刻往里写就是在写系统盘。

---

## 🔗 实战要点
- 安全三件事（最该先会的）：`lsblk -f` 看盘、`df -hT 路径` 确认路径背后的盘、分清 `mount` 与 `mkfs`。
- 写大文件前永远先 `df -hT 目标路径` 确认挂载，别把系统盘写爆。
- 裸盘别擅自 `mkfs`/`fdisk`；fstab 别擅自改——这两件在共享机上属管理员职责。

## 🧠 速记卡
- 链路：物理盘 → 块设备 → 文件系统 → **挂载** → 目录。
- **挂载没成功就写 = 写进系统盘**，最危险的坑。
- `mount` 不毁数据；`mkfs`/`fdisk` 会毁；裸盘先问再动。
- 自动挂载写 `/etc/fstab`（用 UUID）；RAID0 快不安全、RAID1 安全减半、LVM 灵活。
- 查盘：`lsblk -f` / `df -hT 路径` / `blkid`。

## ✅ 自检
1. 一块盘从"物理硬盘"到"能 `cd` 的目录"，中间要经过哪几层？
2. 手动 `mount` 和写 `/etc/fstab` 有什么区别？为什么 fstab 用 UUID？
3. 在共享机上看到一块裸盘，正确的第一反应是什么？
4. RAID0 和 RAID1 各自的取舍是什么？

## ➡️ 收口
至此，「人 · 地 · 权利」三根线 + 多用户协作 + 多盘规划全部接通——你已具备在一台多用户 Linux 服务器上安全干活的完整心智模型。
