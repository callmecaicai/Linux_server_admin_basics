# 第 05 章 · 多盘管理与规划

> **一句话本质**：多盘服务器的核心是三件事——**每块盘干什么（分工）、怎么接进目录树（挂载/fstab）、坏盘了数据还在不在（冗余 RAID/LVM）**。硬盘不会自动变成目录，Linux 靠"挂载"把某个文件系统接到某个路径。

## 📍 这一章在哪
- 前情：第 03 章知道 `/mnt`/`/data` 是"数据地盘的挂载点"，也知道了 inode 是文件的"户口本"。
- 本章：把多块物理盘的规划、挂载、冗余讲透，并接上"inode 会被耗尽""删了文件空间没回来"这些真实坑。
- 贯穿：回扣第 03 章"大文件放数据盘"的原则和 inode 概念。

---

## 1. 存储的分层链路

从物理硬盘到你 `cd` 进去的目录，中间有好几层：

```
物理硬盘
  → 块设备 /dev/nvme0n1            （操作系统眼里的一个"盘"，ls -l 显示 b 开头）
    → 分区 /dev/nvme0n1p1，可选
      → 文件系统 ext4 / xfs        （格式化后才有；它管理 inode 和数据块）
        → 挂载点 /mnt/data1        （接到目录树的某个位置）
          → 你看到的目录和文件
```

- `nvme` = NVMe SSD（高速固态盘），延迟低、吞吐高，适合数据读取、缓存、checkpoint；`sda`/`sdb` 这种 `sd` 开头多是 SATA 盘（机械盘或 SATA SSD）。
- 块设备只是"一个盘"，**还不是你能 `cd` 进去的目录**——要先有文件系统、再挂载。
- **文件系统**这一层就是第 03 章 inode 的归宿：ext4/xfs 在格式化时就划定了能放多少 inode（埋下第 6 节"inode 耗尽"的伏笔）。

**命名规律速记：**
```
/dev/sda          第1块 SATA/SAS 盘     /dev/sda1 它的第1个分区
/dev/sdb          第2块                 /dev/sdb2 它的第2个分区
/dev/nvme0n1      第1块 NVMe 盘         /dev/nvme0n1p1 它的第1个分区
/dev/mapper/xxx   LVM 逻辑卷 / 加密卷    （经过映射层，见第 7 节 LVM）
```

---

## 2. 为什么需要"挂载"

Linux 不像 Windows 天然显示 `C:`、`D:`。它只有**一棵目录树**，从 `/` 开始。硬盘插上也只是个块设备 `/dev/nvme0n1`，本身进不去。

> **"挂载"= 把某块盘上的文件系统，接到目录树的某个位置。**

```bash
sudo mount /dev/nvme0n1p1 /mnt/data1   # 把这个分区的文件系统显示到 /mnt/data1 下
```

- 挂载前：`/mnt/data1` 只是根盘上一个普通空目录。
- 挂载后：`/mnt/data1` 变成那块盘的入口。

> 挂的是**分区**（`nvme0n1p1`）还是**整盘**（`nvme0n1`）取决于这块盘怎么做的：常规做法是先分区再格式化，挂分区；数据盘也有"整盘不分区、直接做一个文件系统"的做法，那就直接挂 `nvme0n1`。`lsblk -f` 一看便知。

> ⚠️ **最常见的坑**：挂载**没成功**就往 `/mnt/data1` 写东西 → 其实写进了**根盘 `/`**，悄悄把系统盘塞满。下数据集、存 checkpoint 最容易踩。**写之前先 `df -hT 目标路径` 确认挂载成功（见第 8 节黄金判断）。**
> 还有个隐蔽变体：盘**已挂载**但你写之后**它又被卸载**，被挂载点"盖住"的那些文件会重新露出来、而你刚写的进了根盘——所以养成"先确认再写大文件"的习惯。

---

## 3. 挂载 vs 格式化（务必分清）

```
mount   挂载：读取已有文件系统，不破坏数据
umount  卸载：把盘从目录树摘下（盘上数据不动）
mkfs    格式化：创建新文件系统，会清掉原有数据 ☠
fdisk   分区：改磁盘结构，也可能破坏数据 ☠
```

> **共享服务器纪律**：看到一块"裸盘"**绝不能直接 `mkfs`**。先确认三件事：这盘是否真没人用、是否允许初始化、要不要组 RAID/LVM。归属与决策通常在管理员手里——**先问，再动。** 一条 `mkfs` 没有"撤销"。

---

## 4. 多盘分工（典型规划）

```
/                系统盘：操作系统 + 基础软件，不塞大文件
/home/alice      用户目录：代码、配置、小文件
/mnt/data1 /data 数据盘：数据集、模型、checkpoint（大文件主战场）
/dev/shm         内存盘(tmpfs)：极快、重启丢失，临时缓存
```

推荐在数据盘下按用途分目录：

```
/data/alice/datasets      数据集
/data/alice/models        预训练模型、缓存（指 HF_HOME 到这）
/data/alice/checkpoints   训练输出
/data/alice/envs          conda 环境
/data/alice/cache         pip/缓存（指 PIP_CACHE_DIR 到这）
```

> 核心原则（回扣第 03 章）：**根盘只放系统和少量工具，大体积内容放挂载盘。** 系统盘一旦写满，轻则服务报错，重则 `sshd`/`systemd` 起不来连机器都登不上——比数据盘满严重得多。

---

## 5. /etc/fstab：开机自动挂载

手动 `mount` 重启就没了。要开机自动挂载，写进 `/etc/fstab`（一行一块盘，六列）：

```
# 设备(用UUID)                              挂载点      类型  选项                  dump  fsck
UUID=abcd-1234-...                          /mnt/data1  ext4  defaults,noatime      0     2
```

六列含义：

| 列 | 例 | 含义 |
|---|---|---|
| ① 设备 | `UUID=...` | 用 **UUID** 而非 `/dev/nvme0n1p1`：设备名会因插槽/枚举顺序变，UUID 永远跟着盘走 |
| ② 挂载点 | `/mnt/data1` | 接到树的哪 |
| ③ 类型 | `ext4` | 文件系统类型 |
| ④ 选项 | `defaults,noatime` | 挂载选项（见下） |
| ⑤ dump | `0` | 备份标记，几乎总是 0 |
| ⑥ fsck 顺序 | `2` | 开机自检顺序：**根盘=1，其他=2，不检=0** |

**常用挂载选项（第④列，值得懂）：**

| 选项 | 作用 |
|---|---|
| `defaults` | 一组常规默认（rw,suid,dev,exec,auto,nouser,async）|
| `noatime` | **不记录 atime**（读文件不再回写时间戳）→ 大幅减少无谓写、提升性能，数据盘强烈推荐 |
| `ro` / `rw` | 只读 / 读写 |
| `nofail` | **挂载失败也不阻止开机**（数据盘建议加，防止盘故障导致整机起不来）|
| `nosuid`/`nodev`/`noexec` | 禁 setuid/设备文件/执行 → 收紧数据盘安全 |

> ⚠️ **fstab 写错可能导致开不了机**（开机自检挂载失败会进 emergency 模式）——生产机改它要格外谨慎：**改完先 `sudo mount -a` 测一遍**（按 fstab 全部挂一次，没报错才算安全），再重启。数据盘加 `nofail` 是很值的保险。这两件通常归管理员。

---

## 6. inode 也会被耗尽（磁盘有空间却"写不下"）

第 03 章说每个文件要占一个 inode。文件系统在**格式化时就固定了 inode 总数**——于是有个反直觉的故障：

> **`df -h` 显示还有几百 G 空闲，却报 `No space left on device`。** 因为**inode 用光了**，不是字节用光了。海量小文件（npm 的 `node_modules`、爬虫缓存、邮件、session 文件）最容易触发。

```bash
df -i              # 看 inode 使用率（IUse% 到 100% 就是它）
df -ih /data       # 人类可读地看某盘 inode
```

- 诊断口诀：报"空间不足"先**同时看 `df -h`（字节）和 `df -i`（inode）**，哪个 100% 是哪个的锅。
- inode 满的解法：删小文件 / 找出谁在狂建小文件（`for d in /data/*; do echo "$(find $d | wc -l) $d"; done` 数文件数）；重建文件系统时可调大 inode 密度（`mkfs.ext4 -i` 或 `-N`），xfs 则动态分配 inode、基本不会耗尽。

---

## 7. RAID / LVM（冗余与灵活）

**RAID（多盘组合，换速度或换安全）：**

| 级别 | 一句话 | 最少盘 | 容量利用 | 坏盘容忍 | 取舍 |
|---|---|---|---|---|---|
| **RAID0** | 多盘拼一块、并行加速 | 2 | 100% | **0（坏一块全丢）** | 纯求速度/临时数据 |
| **RAID1** | 镜像，两块存一样 | 2 | 50% | 1 | 安全，容量减半 |
| **RAID5** | 分布式校验，坏一块能重建 | 3 | (n-1)/n | 1 | 兼顾容量与冗余，写有校验开销 |
| **RAID6** | 双校验，坏两块仍能重建 | 4 | (n-2)/n | 2 | 大阵列首选，重建期更安全 |
| **RAID10** | 先镜像再条带 | 4 | 50% | 每镜像对各 1 | 速度+冗余俱佳，成本高 |

> ⚠️ **RAID 不是备份**：RAID1 把"误删/勒索加密/写坏"**同步到两块盘**，照样全毁；它只防"某块盘物理坏掉"。重要数据该有**独立备份**（异地/快照），别把 RAID 当备份。

**LVM（逻辑卷管理，给存储加一层"软件抽象"）：**

```
物理盘/分区        卷组(VG)            逻辑卷(LV)         文件系统/挂载
/dev/sdb  ┐                        ┌ /dev/vg_data/lv_dataset → /data/datasets
/dev/sdc  ├──→  pool: vg_data ──→  ┤
/dev/sdd  ┘   (把多盘并成一个池)     └ /dev/vg_data/lv_models  → /data/models
   PV              VG                      LV
```

- **PV（物理卷）** 多块盘 →汇入→ **VG（卷组）** 一个大池 →切出→ **LV（逻辑卷）** 当"虚拟分区"用。
- 杀手锏：**在线扩容**。`/data` 快满了，加块新盘 `vgextend` 进池、`lvextend` 撑大 LV、`resize2fs`/`xfs_growfs` 撑大文件系统——**全程不卸载、不停机**。传统分区做不到。
- 还能做**快照**（瞬间冻结一个一致性视图用于备份）。
- 代价：多一层抽象，排查时设备名变成 `/dev/mapper/vg_data-lv_dataset`（别被它吓到，`lsblk` 能看清层级）。

> 深度学习数据盘常见取舍：临时可重建的数据用 RAID0 求速度；重要且难重建的（标注、产出）用 RAID5/6/10 + **额外备份**；要灵活扩容就上 LVM。**别把"快""冗余""备份"三件事混为一谈**——它们各防各的。

---

## 8. 必会的查盘命令

```bash
lsblk -f          # 有哪些盘、有无文件系统、UUID、挂到哪里   [只读，首选总览]
df -hT            # 已挂载文件系统的容量、类型和使用率（字节）[只读]
df -hT /mnt/data1 # 确认某路径背后是哪块盘                  [只读，写大文件前必跑]
df -i             # inode 使用率（空间够却写不下时查它）     [只读]
findmnt           # 树状看当前所有挂载关系                  [只读，最清晰]
blkid             # 看 UUID（写 fstab 用）                  [只读]
du -sh *          # 看当前目录各项各占多少空间              [只读，找谁占地]
du -sh /data/* | sort -h   # 排序找出最占地的目录          [只读]
sudo mount -a     # 按 fstab 全挂一遍（改完 fstab 必测）    [验证用]
```

> **黄金判断**：`df -hT /mnt/data1` 若显示**数据盘设备**（如 `/dev/nvme0n1p1`）、容量符合 → 正在用数据盘；若显示的是**根盘设备**（如 `/dev/sda2`、`/dev/nvme0n1p2`，或用了 LVM 的 `/dev/mapper/...`、容量是系统盘大小）→ **没挂载成功**，此刻往里写就是在写系统盘。

---

## 9. 实战坑：`rm` 了大文件，磁盘空间却没回来

回扣第 03 章 inode 的释放规则：**inode（及其数据块）只有在"没有任何文件名指着它"且"没有任何进程开着它"时才真正释放。** 于是一个高频事故：

> 日志 `/var/log/app.log` 涨到 50G、`df` 显示盘满。你 `rm app.log`，**`df` 却纹丝不动**。因为**进程（如还在运行的 app）仍开着那个已删文件的句柄** → inode 没释放 → 空间不还。

```bash
# 诊断：找出"已删除但仍被进程占用"的文件
sudo lsof | grep deleted
# 输出里能看到进程名、PID 和那个 (deleted) 的大文件
```

**解法**：① 重启/优雅重载那个进程（句柄释放，空间立刻回来）；② 或对日志用 `> /var/log/app.log` 截断（把内容清空但不删 inode，进程继续写新内容）——这正是 `logrotate` 用 `copytruncate` 的原理。

> 记牢这条因果链，你就同时理解了：为什么 `rm` 大文件后要重启进程、为什么 truncate 比 rm 更适合在线清日志、以及第 03 章 inode 那套抽象的现实意义。

---

## 10. 网络存储：NFS 与集群文件系统（算力机/超算场景）

前面讲的都是"插在这台机器上的盘"。但在**算力集群/超算**上，你会遇到另一种盘——**根本不在本机、通过网络挂进来的**：

```
存储服务器 storage:/export/home ──网络──▶ 挂到每个计算节点的 /home
                                          （node1、node2、node3 看到的是同一份）
```

这就是 **NFS（Network File System，网络文件系统）**。它解释了集群上几个"理所当然却没人讲"的现象：

| 现象 | 原因 |
|---|---|
| 登 node1 和 node3，家目录**一模一样** | `/home` 是 NFS：所有节点挂的是**存储服务器上同一份** |
| 在 A 节点写的 checkpoint，B 节点立刻能读 | 同上——根本不是"同步"，就是同一份 |
| `df -hT /home` 的类型是 `nfs4`、设备列长得像 `storage:/export/home` | **设备列带冒号（`主机:路径`）= 网络盘**；`ext4/xfs` 才是本地盘 |
| 集群要求所有节点**统一 UID**（用 LDAP/NIS 名单） | NFS 按 **UID 数字**记账（第 01 章）：UID 不统一，你在 A 节点的文件到 B 节点就成了"别人的" |
| 计算节点上 `sudo` 也动不了 NFS 上别人的文件 | NFS 默认 **root_squash**：客户端的 root 被降级为 nobody——防"攻陷一个节点就拿下全部存储" |
| **conda/pip 装在 NFS 家目录慢得要死** | conda 环境是**海量小文件**，NFS 每个文件都要走一轮网络往返——小文件是 NFS 的死穴（大文件顺序读写反而还行）|

**超算的进阶版：并行文件系统 + scratch 纪律**

超算把"网络共享盘"做到极致——**Lustre / GPFS** 这类**并行文件系统**：数据条带化分布在几十台存储机上、所有节点并行读写，专为大文件高吞吐设计。于是典型超算的存储分工是：

```
/home     NFS：小、稳、有配额、有备份     → 代码、配置、小文件
/scratch  Lustre/GPFS：大、极快、无备份   → 数据集、checkpoint（大文件主战场）
          ⚠️ 而且通常【定期自动清理】（如 30 天未访问即删）
本地盘/SSD（如 /tmp、$TMPDIR）：每节点独立  → 训练时的临时高速缓存
```

> ⚠️ **scratch 纪律（吃过亏才懂）**：`/scratch` 是"草稿区"——快、但**不备份且会定期清**。跑完的重要产出**及时拷回 /home 或下载走**，别让 30 天清理把你的 checkpoint 收走。各家超算的清理周期写在它的用户文档里，上机先查这条。

实操判断（与第 8 节黄金判断合体）：

```bash
df -hT 路径      # Type 列：ext4/xfs=本地盘；nfs4=NFS；lustre/gpfs=并行文件系统
                # 设备列带冒号(storage:/export)=网络盘
```

> 选地盘口诀（CV/深度学习版）：**海量小文件（conda env、代码仓）尽量放本地盘或 /home；大文件吞吐（数据集、checkpoint）放 scratch/并行文件系统；临时中间结果用节点本地 SSD**。放反了（conda 进 Lustre、数据集塞 NFS /home）就是"环境激活半分钟、训练 IO 瓶颈"的由来。

---

## 🔗 实战要点
- 安全三件事（最该先会的）：`lsblk -f` 看盘、`df -hT 路径` 确认路径背后的盘、分清 `mount` 与 `mkfs`。
- 写大文件前永远先 `df -hT 目标路径` 确认挂载，别把系统盘写爆。
- 报"空间不足"先**同时看 `df -h` 和 `df -i`**——可能是 inode 满而非字节满。
- `rm` 大文件后 `df` 不回收 → `lsof | grep deleted` 找占用进程，重启它或改用 truncate。
- 裸盘别擅自 `mkfs`/`fdisk`；fstab 改完先 `mount -a` 再重启——这几件在共享机上属管理员职责。

## 🧠 速记卡
- 链路：物理盘 → 块设备 → (分区) → **文件系统** → **挂载** → 目录；命名 `sd*`=SATA、`nvme*`=NVMe、`mapper/*`=LVM。
- **挂载没成功就写 = 写进系统盘**，最危险的坑；写前 `df -hT 路径` 确认。
- `mount` 不毁数据；`mkfs`/`fdisk` 会毁；裸盘先问再动。
- 自动挂载写 `/etc/fstab`（用 **UUID**，加 `noatime`/`nofail`，改完 `mount -a` 测）。
- **inode 会单独耗尽**：空间够却写不下 → `df -i` 查。
- RAID0 快不安全 / RAID1 镜像减半 / RAID5 坏一块 / RAID6 坏两块 / RAID10 兼顾；**RAID≠备份**。LVM = PV→VG→LV，在线扩容+快照。
- `rm` 大文件空间没回来 → 进程还开着它 → `lsof | grep deleted`，重启进程或 truncate。
- 集群盘：**NFS**=网络挂载同一份（`df -hT` 设备带冒号；按 UID 对账；root_squash；**小文件慢**）；超算 **/home(NFS·稳) + /scratch(Lustre·快·会定期清！)**；conda 别进网络盘。

## ✅ 自检
1. 一块盘从"物理硬盘"到"能 `cd` 的目录"，中间要经过哪几层？文件系统那层管什么？
2. 手动 `mount` 和写 `/etc/fstab` 有什么区别？为什么 fstab 用 UUID、改完要 `mount -a`？
3. `df -h` 显示还有 500G，却报 No space left，怎么回事？敲什么命令确认？
4. RAID1 能当备份用吗？为什么？RAID5 和 RAID6 差在哪？
5. LVM 的 PV/VG/LV 三层各是什么？它最大的好处是什么？
6. `rm` 掉一个 50G 日志后 `df` 没变化，原因是什么？两种解法？
7. 集群上每个节点的 `/home` 为什么一模一样？怎么用 `df -hT` 区分本地盘和网络盘？
8. conda 环境装在 NFS 家目录为什么慢？超算上 `/home` 和 `/scratch` 该怎么分工、scratch 有什么纪律？

## ➡️ 收口
至此，「人 · 地 · 权利」三根线 + 多用户协作 + 多盘规划全部接通——你已具备在一台多用户 Linux 服务器上安全干活的完整心智模型。**遇到具体报错怎么办？** 翻配套的《Linux 排查指南》，把这套原理变成"Permission denied / 磁盘满 / 加组没生效"的确定性诊断流程。
