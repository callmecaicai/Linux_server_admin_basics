# 第 00 章 · 总纲：管人 · 管地 · 管权利

> **一句话本质**：服务器管理无论多复杂，只管三样东西——**人（用户/组）、地（存储/目录）、人对地的权利（权限）**，而这三样**全部交汇在 `ls -l` 的每一行**里。读懂那一行，你就握住了全部。

## 📍 这一章在哪
- 前情：你会用 `ssh` 进机器，但里面"多人共享、目录林立、权限报错"还是一团雾。
- 本章：给整套笔记一根脊柱，之后每一章都是这根脊柱上的一节。
- 贯穿：所有后续章节（用户、权限、目录、多用户、多盘）都回扣这里的"三样东西 + 三个追问"。

---

## 1. 一根脊柱：人 · 地 · 权利

```
   管「人」          管「地」              管「人对地的权利」
   ┌────────┐      ┌──────────┐         ┌──────────────┐
   │  用户   │      │  存储/目录 │         │     权限       │
   │ user   │      │ 目录架构   │         │ owner/rwx     │
   │ group  │      │ 磁盘/挂载  │         │ chmod/chown   │
   └───┬────┘      └────┬─────┘         └──────┬───────┘
       └────────────────┴───────────────────────┘
                         ▼
            全部交汇在一行：  ls -l 的输出
```

- **人**：机器认得谁？怎么把人分队？（第 01 章）
- **权利**：谁能读/写/执行某样东西？（第 02 章）
- **地**：这些东西摆在机器的哪个目录、哪块盘？（第 03 / 05 章）
- **协作**：多人同时在一块地上干活怎么不打架？（第 04 章）

---

## 2. 一行 `ls -l` 的完整解剖

整套笔记都在解释下面这一行的每一段：

```
-rw-r--r--   2   alice   ml    4096   Jun  9 16:00   model.pt
└───┬────┘   │   └─┬─┘  └┬─┘  └─┬─┘  └────┬─────┘   └──┬──┘
  ① 类型+权限  ②   ③属主  ④属组  ⑤大小    ⑥修改时间      ⑦名字
            硬链接数
```

| 段 | 含义 | 哪一章讲透 |
|---|---|---|
| ① 第 1 个字符 | **类型**（见下表） | 02 / 03 |
| ① 后 9 个字符 | 权限 `rwx`×3 档（owner/group/other）| 02 |
| ② 硬链接数 | 有几个"名字"指着同一份数据（inode）| 03 |
| ③ 属主 | 这块"地"归**谁**（user）| 01 |
| ④ 属组 | 归哪个**队**（group）——注意属主和属组**可以不同名**（如 `alice` 的文件归 `ml` 组）| 01 / 04 |
| ⑤ 大小 | 字节数（目录显示的是目录本身的大小，**不是**内容总和）| 05 |
| ⑥ 修改时间 | mtime（内容最后改动时间）| 见下文 |
| ⑦ 名字 | 文件/目录名 | 03 |

**第 1 个字符的全家福**（不止 `-` 和 `d`）：

| 字符 | 类型 | 例子 |
|---|---|---|
| `-` | 普通文件 | `model.pt`、脚本、日志 |
| `d` | 目录 | `/home/alice` |
| `l` | 软链接（指路牌）| `/usr/bin/python -> python3.10` |
| `b` | 块设备 | `/dev/nvme0n1`（磁盘！第 05 章主角）|
| `c` | 字符设备 | `/dev/null`、终端 |
| `s` | socket | docker 的 `/var/run/docker.sock` |
| `p` | 命名管道 | 进程间通信用 |

> **"一切皆文件"**：磁盘、终端、socket 在 Linux 眼里都是文件，都躺在目录树里、都受同一套"人/地/权利"管辖。这就是为什么"进不了 docker 组就用不了 docker"——本质是你对 `/var/run/docker.sock` 这个文件没有 rw（第 01 章组、第 02 章权限的联合应用）。

**时间戳其实有三兄弟**（`ls -l` 默认只显示 mtime）：

| 时间戳 | 记录什么 | 怎么看 |
|---|---|---|
| **mtime** | 内容最后修改 | `ls -l`（默认）|
| **atime** | 最后被读取 | `ls -lu` |
| **ctime** | 元数据（权限/属主）最后变更 | `ls -lc` |

> 想一次看全？用 **`stat 文件名`**——它把 inode 号、三个时间戳、权限的**八进制数字形式**（如 `0644`）全部摊开，是 `ls -l` 的"完整体检报告"。

---

## 3. 三主体：机器看人只分三档

权限永远是说给**三档人**听的，不多不少：

| 档 | 是谁 | 符号 |
|---|---|---|
| **owner（属主）** | 这东西的主人（通常=创建者）| `u` |
| **group（同组）** | 和这东西**属组**相同的人（不是"和主人同组"，是和**文件的组**同组）| `g` |
| **other（其他所有人）** | 既不是属主、又不在属组里的所有人 | `o` |

> 你面对任何文件，机器**从左到右对号入座、命中即停**：你是属主？→ 只看 owner 档。不是 → 你在文件的属组里？→ 只看 group 档。也不在 → 按 **other** 档放行或拒绝。
>
> "命中即停"有个反直觉推论（第 02 章细讲）：如果一个文件是 `----rwxrwx alice ml`，那么 **alice 自己反而读不了**——她命中了 owner 档，owner 是 `---`，机器不会再"退一档"去看 group。

---

## 4. 第四个隐形主角：进程（权限的真正主语）

前面说"**你**能不能读这个文件"，严格说不准确。机器上真正干活的是**进程**：

> **每个进程都顶着一个用户身份（UID）在跑；权限检查的主语是进程，不是坐在键盘前的你。**

- 你敲 `cat model.pt` → 启动一个 `cat` 进程，它**继承你的 UID** → 内核拿这个 UID 去对文件做"三档对号"。
- `sudo cat /etc/shadow` → 这个 `cat` 进程顶着 **root（UID 0）** 在跑 → 检查全过。所以第 04 章说 **sudo 是"换人"**——精确说是"让这条进程换一个 UID 跑"。
- nginx、sshd 这些服务也各自顶着自己的专属用户跑（第 01 章"系统用户"）——服务被攻破时，攻击者也只拿到那个低权用户的权利。

**root（UID 0）是检查规则的"超级例外"**：内核对 root 进程几乎跳过所有 rwx 检查（只有"执行"还要求文件至少有一档带 `x`）。这就是 root 既万能又危险的原因——规则对它不设防。

---

## 5. 三个追问（诊断任何权限问题的钥匙）

遇到任何"我能不能动这个东西""为什么报 Permission denied"，照这三句走：

1. **谁**？ → `id`、`whoami`：我（的进程）是哪个 user、在哪些 group。
2. **什么权利**？ → `ls -l`：这东西对 owner/group/other 各开了哪几位 rwx，我落在哪一档。
3. **在哪块地**？ → `pwd`、看路径：在系统地盘（属 root、我只读）还是数据地盘（我的，可写）；删不掉还要看**所在目录**的 w；整条路径上每一级目录都要有 x 才进得去（第 02 章）。

```
排查起手式（30 秒）：
whoami          # 我是谁
id              # 我在哪些组
ls -l  目标文件  # 它的权限/属主/属组
ls -ld 所在目录  # 目录的权限（删/建文件看这里）
```

---

## 🔗 实战要点
- 进一台新机器/排查权限，**第一组命令**永远是：`whoami` → `id` → `pwd` → `ls -l`。一口气回答完三个追问。
- 多数"权限问题"本质是：**你被对号到了 other 档，而 other 没开你要的那位**。
- 第二常见：**目录链上某一级没有 `x`**，你连"走到文件跟前"都做不到（第 02 章详解，排查指南有决策树）。
- `ls -l` 看不清时换 `stat`：八进制权限、三个时间戳、inode 一目了然。

## 🧠 速记卡
- 三样东西：**人 / 地 / 权利**，全交汇于 `ls -l` 一行。
- `ls -l` 七段：**类型权限 / 硬链接数 / 属主 / 属组 / 大小 / mtime / 名字**。
- 三档人：**owner / group / other**，对号入座、**命中即停不退档**。
- 权限的真正主语是**进程**（顶着 UID 跑）；root（UID 0）= 检查的超级例外。
- 三个追问:**谁 → 什么权利 → 在哪块地**;起手式 `whoami → id → pwd → ls -l / ls -ld`。

## ✅ 自检
1. `ls -l` 一行里，哪一段是"人"、哪一段是"权利"、哪一段是"地"？第二列的数字是什么？
2. 一个陌生人来访问你的文件，机器按什么顺序把他对号入座？"命中即停"会造成什么反直觉现象？
3. 报 Permission denied，你会依次问哪三个问题、敲哪几条命令？
4. 为什么说权限检查的主语是"进程"而不是"人"？sudo 在这个视角下是什么？
5. `/dev/nvme0n1` 的 `ls -l` 第一个字符是什么？这体现了 Linux 的什么哲学？

## ➡️ 下一章
先从"人"讲起：`ssh` 进去敲 `whoami` 回你一个用户名——机器到底**怎么认识你的**？UID 0 和 UID 1000 有什么本质区别？nginx 为什么也算一个"用户"？下一章《用户与组》。
