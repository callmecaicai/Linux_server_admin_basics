# 第 00 章 · 总纲：管人 · 管地 · 管权利

> **一句话本质**：服务器管理无论多复杂，只管三样东西——**人（用户/组）、地（存储/目录）、人对地的权利（权限）**，而这三样**全部交汇在 `ls -l` 的每一行**里。读懂那一行，你就握住了全部。

## 📍 这一章在哪
- 前情：你会用 `ssh` 进机器，但里面"多人共享、目录林立、权限报错"还是一团雾。
- 本章：给整套笔记一根脊柱，之后每一章都是这根脊柱上的一节。
- 贯穿：所有后续章节（用户、权限、目录、多用户、多盘）都回扣这里的"三样东西 + 三个追问"。

---

## 1. 一根脊柱：人 · 地 · 权利

```
   管「人」          管「地」              管「人对地的权利」
   ┌────────┐      ┌──────────┐         ┌──────────────┐
   │  用户   │      │  存储/目录 │         │     权限       │
   │ user   │      │ 目录架构   │         │ owner/rwx     │
   │ group  │      │ 磁盘/挂载  │         │ chmod/chown   │
   └───┬────┘      └────┬─────┘         └──────┬───────┘
       └────────────────┴───────────────────────┘
                         ▼
            全部交汇在一行：  ls -l 的输出
```

- **人**：机器认得谁？怎么把人分队？（第 01 章）
- **权利**：谁能读/写/执行某样东西？（第 02 章）
- **地**：这些东西摆在机器的哪个目录、哪块盘？（第 03 / 05 章）
- **协作**：多人同时在一块地上干活怎么不打架？（第 04 章）

---

## 2. 一行 `ls -l` 的完整解剖

整套笔记都在解释下面这一行的每一段：

```
-rw-r--r--   1   alice   alice    4096    Jun  9 16:00   model.pt
└───┬────┘   │   └─┬─┘   └─┬─┘   └─┬─┘                  └──┬──┘
  ① 类型+权限 │   ② 属主   ③ 属组   ④ 大小                   ⑤ 名字
            (链接数)
```

| 段 | 含义 | 哪一章讲透 |
|---|---|---|
| ① 第 1 个字符 | 类型：`-`普通文件 / `d`目录 / `l`软链接 | 02 / 03 |
| ① 后 9 个字符 | 权限 `rwx`×3 档（owner/group/other）| 02 |
| ② 属主 | 这块"地"归**谁**（user）| 01 |
| ③ 属组 | 归哪个**队**（group）| 01 |
| ④ 大小、时间 | 元数据 | — |
| ⑤ 名字 | 文件/目录名 | 03 |

> **记住这一行的结构**，后面所有"权限报错、归属混乱、删不掉、读不到"都能在这一行里定位。

---

## 3. 三主体：机器看人只分三档

权限永远是说给**三档人**听的，不多不少：

| 档 | 是谁 | 符号 |
|---|---|---|
| **owner（属主）** | 这东西的主人（通常=创建者）| `u` |
| **group（同组）** | 和主人同一个组的人 | `g` |
| **other（其他所有人）** | 既不是主人、又不在组里的所有人 | `o` |

> 你面对任何文件，机器先问：你是属主？不是→你是同组？不是→那你就是 **other**。对号入座，按那一档的权限放行。

---

## 4. 三个追问（诊断任何权限问题的钥匙）

遇到任何"我能不能动这个东西""为什么报 Permission denied"，照这三句走：

1. **谁**？ → `id`、`whoami`：我是哪个 user、在哪些 group。
2. **什么权利**？ → `ls -l`：这东西对 owner/group/other 各开了哪几位 rwx，我落在哪一档。
3. **在哪块地**？ → `pwd`、看路径：在系统地盘（属 root、我只读）还是数据地盘（我的，可写）；删不掉还要看**所在目录**的 w。

---

## 🔗 实战要点
- 进一台新机器/排查权限，**第一组命令**永远是：`whoami` → `id` → `pwd` → `ls -l`。一口气回答完三个追问。
- 多数"权限问题"本质是：**你被对号到了 other 档，而 other 没开你要的那位**。

## 🧠 速记卡
- 三样东西：**人 / 地 / 权利**，全交汇于 `ls -l` 一行。
- 三档人：**owner / group / other**，对号入座。
- 三个追问：**谁 → 什么权利 → 在哪块地**。
- 排查起手式：`whoami → id → pwd → ls -l`。

## ✅ 自检
1. `ls -l` 一行里，哪一段是"人"、哪一段是"权利"、哪一段是"地"？
2. 一个陌生人来访问你的文件，机器按什么顺序把他对号入座？
3. 报 Permission denied，你会依次问哪三个问题、敲哪几条命令？

## ➡️ 下一章
先从"人"讲起：`ssh` 进去敲 `whoami` 回你一个用户名——机器到底**怎么认识你的**？下一章《用户与组》。
