# Linux 服务器排查指南

> **一句话定位法 + 命令工具箱 + 场景速查。** 不堆命令清单，而是把前 6 章原理变成**确定性流程**：任何"权限报错 / 磁盘满 / 加组没生效 / 进不去目录"，都用同一套"三个追问"在 5 分钟内定位。
>
> 配套阅读：原理见 `00`–`05` 各章，本篇是它们的**逆向诊断版**。

---

## 0. 30 秒快速开始（遇到问题先跑这里）

别急着 `sudo`。先回答第 00 章的**三个追问**，90% 的问题一眼定位：

```bash
whoami           # ① 谁：我是哪个用户
id               # ① 谁：我在哪些组（看 sudo/docker/目标组在不在）
ls -l   目标       # ② 什么权利：它的 rwx / 属主 / 属组
ls -ld  所在目录    # ② + ③：目录权限（删/建文件看这里）、在哪块地
df -hT  所在路径    # ③ 在哪块地：哪块盘、还有空间吗
```

然后问自己**唯一一个决定性问题**：

> **报错是"权利不够"还是"地没了"？**
> - **Permission denied / Operation not permitted** → 权利问题（跳第 2 节）。
> - **No space left / Read-only file system / Disk quota exceeded** → 地的问题（跳第 4 节）。
> - **明明加了组/用户却不认** → 身份生效问题（跳第 5 节）。

---

## 1. 核心心法：一切落到"人 · 地 · 权利"

> **任何 Linux 操作失败，本质都是"某个进程，顶着某个 UID，对某块地，缺某一位权利"。排查 = 把这句话的四个空填出来。**

```
进程(顶着 UID/GID) ──试图──▶ 对某个文件/目录 ──做── 读/写/执行/删
       ①谁                        ③在哪块地              ②什么权利
```

四个空一填，问题自己就显形了。下面按最高频的三类故障，各给一棵决策树。

---

## 2. 「Permission denied」决策树（最高频）

看到 `Permission denied`，**别急着 sudo**——先沿这棵树走，多数时候根本不需要 root，需要的也能精准知道该改哪一位。

```
Permission denied
  │
  ├─ ① 我是谁？  id
  │     └─ 我是文件属主吗？在文件的属组里吗？→ 确定我落 owner/group/other 哪一档
  │
  ├─ ② 我落的那一档，有我要的那一位吗？  ls -l 文件
  │     ├─ 读不了 → 那档有 r 吗
  │     ├─ 写不了 → 那档有 w 吗
  │     └─ 执行不了 → 那档有 x 吗（脚本还要看有没有 x）
  │     ⚠️ 命中即停：我是 owner 但 owner 档没开 → 直接拒，不会退到 group/other（第02章）
  │
  ├─ ③ 文件够得着吗？路径上每一级目录都有 x 吗？  namei -l /全/路径/文件
  │     └─ 任意一级目录缺 x → 还没走到文件跟前就被拦（隐形元凶）
  │
  ├─ ④ 要"删/改名/新建"？看的是【目录】的 w，不是文件的！  ls -ld 所在目录
  │     └─ 文件是你的但目录没 w → 删不掉；目录有 sticky(t) → 只能删自己当属主的
  │
  └─ ⑤ 以上 rwx 都正常却还失败？
        ├─ 文件被锁：  lsattr 文件   → 有 i(immutable) 连 root 都改不了，先 chattr -i
        ├─ 有 ACL：    getfacl 文件  → ls -l 末尾有 + 号，真实权限要看 ACL
        └─ 挂载只读：  报 "Read-only file system" → mount 看选项，盘可能 ro 或故障转 ro
```

### 招牌命令：`namei -l` 一键查整条路径

"文件明明 `644` 可读，就是 `cat` 不出来"——十有八九是**路径上某级目录缺 x**。`namei -l` 把整条路径每一级的权限摊开，一眼看到断在哪：

```bash
namei -l /data/alice/secret/model.pt
# f: /data/alice/secret/model.pt
#  drwxr-xr-x root  root   /
#  drwxr-xr-x root  root   data
#  drwx------ alice alice  alice     ← 断在这！这级只有 owner 能进，你不是 alice 就穿不过
#  drwxr-xr-x alice alice  secret
#  -rw-r--r-- alice alice  model.pt  ← 文件本身可读，但你根本走不到这
```

> 口诀：**文件权限对、却读不到 → `namei -l` 查穿过权**。比一级一级 `ls -ld` 快十倍。

### 招牌场景速查

| 现象 | 真因 | 一句话解 |
|---|---|---|
| 文件可读却 `cat` 报拒 | 路径某级目录缺 x | `namei -l 全路径`，给缺 x 的目录补 `chmod o+x` 或 `g+x` |
| 我的文件却删不掉 | **所在目录**没 w | `ls -ld 目录`；删/建看目录 w，不是文件权限 |
| `/tmp` 里删不掉别人的 | sticky bit | 正常行为，只能删自己当属主的 |
| 我是 owner 却读不了 | owner 档没 r（命中即停不退档）| `chmod u+r 文件` |
| root 都改不了 | `chattr +i` 锁了 | `lsattr` 查，`sudo chattr -i` 解 |
| `ssh` 报 `permissions too open` | 私钥太松 | `chmod 600 私钥`、`chmod 700 ~/.ssh` |
| 装包 Permission denied | 写的是系统地盘（`/usr`）| 用 conda/虚拟环境装到家目录（第03章），或确需系统包才 sudo |
| 进 docker 报权限 | 不在 docker 组 | `id` 查；`usermod -aG docker` 后**重登**（第5节）|
| `Operation not permitted`（≠denied）| 多是 capability/属主限制 | 如改别人文件属主需 root；或容器缺 capability |

---

## 3. 「读不到 / 进不去」二分缩小

定位到大概是权限后，用**对比**精准缩小——改变一个变量，看结果变不变：

| 二分维度 | 怎么做 | 结论 |
|---|---|---|
| **换身份** | `sudo` 同一条命令试 | sudo 行=纯权限问题；sudo 也不行=文件被锁(`+i`)/挂载 ro/路径不存在 |
| **换文件** | 同目录另一个文件试 | 都不行=目录问题（穿过权/挂载）；只这个不行=这个文件的权限/ACL |
| **换目录** | 把文件复制到 `/tmp` 再试 | 到 `/tmp` 就行=原路径穿过权问题 |
| **换用户** | `sudo -u bob 命令` 模拟 bob | 定位"是不是某个特定用户的归属/组问题" |
| **看真相** | `stat 文件` + `namei -l 路径` + `getfacl 文件` | 八进制权限、整条路径、ACL 一次看全 |

---

## 4. 「磁盘满 / 写不下」决策树

报 `No space left on device`，**先分清是"字节满"还是"inode 满"**——这是最容易误判的一刀：

```
No space left on device
  │
  ├─ 先同时看两个！
  │     df -h   路径    → 字节用满了吗（Use% = 100%?）
  │     df -i   路径    → inode 用满了吗（IUse% = 100%?）
  │
  ├─ A. 字节满（df -h 100%）
  │     ├─ 谁占的？  du -sh /路径/* | sort -h   （从大到小，揪出大户）
  │     ├─ 删了却没回来？  sudo lsof | grep deleted
  │     │      └─ 进程还开着已删文件 → 重启该进程，或 truncate（见下）
  │     └─ 写错盘了？  df -hT 路径 看是不是写进了根盘（挂载没成功，第05章黄金判断）
  │
  ├─ B. inode 满（df -i 100%，但 df -h 还有空间）
  │     ├─ 海量小文件！哪个目录？
  │     │      for d in /data/*; do printf "%8s %s\n" "$(find "$d" -xdev | wc -l)" "$d"; done | sort -rn
  │     └─ 清掉小文件堆（node_modules/缓存/session），或迁到 xfs（inode 动态分配）
  │
  └─ C. 报 "Read-only file system"
        └─ 文件系统出错被内核转为只读保护 → dmesg 看磁盘报错，可能盘坏，别硬写
```

### 招牌坑①：`df` 和 `du` 对不上

```bash
df -h /data        # 说用了 90%
du -sh /data       # 加起来却只有 50%——差的 40% 去哪了？
```
三种可能：
1. **被删但仍被进程占用的文件**（最常见）：`sudo lsof | grep deleted` → 重启占用进程，空间立刻回来。
2. **挂载点被盖住**：某目录原本有文件，又在它上面挂了别的盘 → 底下的文件 `du` 数不到但仍占空间。`sudo umount` 后或用 `findmnt` 看清。
3. `du` 默认不算隐藏的、或跨了文件系统边界 → 加 `du -sh --one-file-system` 限定。

### 招牌坑②：删了大日志，空间不回来

```bash
# 现象：rm 了 50G 的 app.log，df 纹丝不动
sudo lsof | grep deleted          # 找到还开着它的进程（app, PID 12345）
# 解法二选一：
#  ① 重启/reload 那个进程 → 句柄释放，空间回来
#  ② 别 rm，改用截断（进程继续写，空间立即回收）：
: > /var/log/app.log              # 把文件清空但保留 inode（logrotate copytruncate 同理）
```

> 根因（第 03/05 章）：inode 只在"无文件名指向 **且** 无进程打开"时才释放。`rm` 只去掉名字，进程的句柄还按着它。

---

## 5. 「加了组/用户却不生效」决策树

```
明明 usermod -aG 了，却还是没权限
  │
  ├─ ① 当前会话还是旧身份！  id
  │     └─ 看不到新组 = 组成员在【登录那一刻】载入进程，加组后必须【重新登录】
  │        临时验证：newgrp 组名（只对当前这个子shell生效）
  │
  ├─ ② 命令写错把组清了？  回看命令
  │     └─ usermod -G docker alice（漏了 a）会重置附加组、踢出 sudo/ml 等！
  │        正确永远是 usermod -aG（第01章）
  │
  ├─ ③ 组对了，但目标文件/目录没给这个组开权？  ls -l / ls -ld
  │     └─ 加进 ml 组 ≠ 自动能写；还要目标的 group 档有 rwx（第02章）
  │
  └─ ④ 共享目录"新文件"组员仍不能写？  → 第04章完整配方没走完
        ├─ 目录有 setgid 吗？  ls -ld 看 s 位（drwxrws---）
        └─ 新文件组带 w 吗？   umask 是不是 022 把组 w 挖了 → 改 002 或设默认 ACL
```

> 排查顺序固定：**重登了吗 → 命令漏 a 没 → 目标给组开权没 → 共享目录配方走全没**。90% 卡在第一步"没重登"。

---

## 6. 每类对象的"体检命令"工具箱

定位到具体对象后，用这一组把它彻底看穿：

```bash
# —— 看文件/目录的全部真相 ——
stat 文件                 # inode、八进制权限、三个时间戳、大小，一次看全
ls -ld 目录               # 目录本身的权限（不是里面内容）
namei -l /全/路径          # 整条路径每级的权限+属主（查穿过权神器）
getfacl 文件              # ACL（ls -l 末尾有 + 号时必看）
lsattr 文件              # 文件系统属性（i=immutable, a=append-only）
readlink -f 路径          # 一路解软链接，看最终指向的真身

# —— 看身份 ——
id [用户]                 # UID/GID/全部组
getent passwd 用户        # 查用户（含 LDAP/网络用户，比 cat passwd 全）
getent group 组名         # 查组里有谁
sudo -l                  # 我能 sudo 哪些命令（sudoers 授权）

# —— 看磁盘/挂载 ——
df -hT 路径               # 路径背后是哪块盘、容量（字节）
df -i 路径               # inode 使用率
findmnt                  # 树状看所有挂载关系
lsblk -f                 # 盘/分区/文件系统/挂载点总览
du -sh /路径/* | sort -h  # 谁占地
sudo lsof | grep deleted # 已删但被占用的文件

# —— 看进程顶着谁在跑 ——
ps -eo pid,user,group,comm   # 每个进程的身份
```

---

## 7. 分场景速查（按真实场景）

**🔹 进不去目录 / 文件读不到**
- `namei -l 全路径` → 找穿过权断在哪一级 → 给那级目录补 x。
- 文件本身权限对就别动文件，问题在路径上的目录。

**🔹 删不掉文件**
- `ls -ld 所在目录` 看目录有没有 w（删文件看目录！）。
- `/tmp` 等带 `t` 的目录只能删自己的，正常。
- root 都删不掉 → `lsattr` 查 `+i`，`chattr -i` 解锁。

**🔹 磁盘满**
- `df -h` 和 `df -i` 都看（字节满 vs inode 满）。
- `du -sh /* | sort -h` 揪大户；`lsof | grep deleted` 找"删了没回收"的。
- 确认没写错盘：`df -hT 目标路径` 别是根盘（挂载没成功）。

**🔹 加组没权限**
- `id` 看新组在不在 → 不在就**重登**。
- 检查当初命令有没有漏 `usermod -aG` 的 `a`。

**🔹 共享目录组员不能改新文件**
- `ls -ld` 看 setgid（`s`）→ 再查 umask/默认 ACL（第 04 章完整配方）。

**🔹 ssh 连上了但报私钥权限**
- `chmod 600 ~/.ssh/id_*`、`chmod 700 ~/.ssh`、`chmod 644 ~/.ssh/authorized_keys`。

**🔹 装包/写系统目录被拒**
- 你写的是系统地盘（`/usr` 等）。优先 conda/venv 装家目录（免 sudo）；确需系统级才 `sudo`。

**🔹 容器里权限怪异**
- 容器内 UID 和宿主 UID 是**同一套数字、不同名字**：宿主 `1000` 的文件，容器里可能显示别的名字甚至 root。挂载卷的权限按 **UID 数字**对，不按名字。`ls -ln`（显示数字 UID）排查最准。

---

## 8. 总流程图（脑内跑这一遍）

```
操作失败
  │
  ├─ Permission denied / Operation not permitted ──▶ 权利问题
  │     ├─ id：我落 owner/group/other 哪档？（命中即停）
  │     ├─ ls -l：那档有我要的 r/w/x 吗？
  │     ├─ namei -l：路径每级都有 x 吗？（穿过权）
  │     ├─ 删/建？ → ls -ld 看【目录】的 w
  │     └─ rwx 都对 → lsattr(+i) / getfacl(ACL) / 挂载 ro
  │
  ├─ No space left ──────────────────────────────▶ 地的问题
  │     ├─ df -h 满？ → du 揪大户 / lsof|grep deleted / 是否写错根盘
  │     └─ df -i 满？ → inode 耗尽（海量小文件）
  │
  ├─ 加组/用户不认 ──────────────────────────────▶ 身份生效问题
  │     └─ id 看新组 → 没有就重登；查命令漏没漏 -aG 的 a
  │
  └─ 全程牢记：谁(UID) · 对哪块地 · 缺哪一位权利 —— 四个空填出来就破案
```

---

## 9. 一页纸速查卡（遇事照这个跑）

```
┌─────────────────────────────────────────────────────────────┐
│  Linux 排查速查卡                                            │
├─────────────────────────────────────────────────────────────┤
│  万能首查:  whoami / id / ls -l 目标 / ls -ld 目录 / df-hT 路径│
├─────────────────────────────────────────────────────────────┤
│  Permission denied:                                          │
│   ① id        → 我落 owner/group/other 哪档（命中即停不退档） │
│   ② ls -l     → 那档有 r/w/x 吗                              │
│   ③ namei -l  → 路径每级有 x 吗（穿过权·读不到文件首查）      │
│   ④ 删/建?    → ls -ld 看【目录】的 w（不是文件！）          │
│   ⑤ 还失败?   → lsattr(+i) / getfacl(ACL+) / 挂载 ro         │
├─────────────────────────────────────────────────────────────┤
│  磁盘满:  df -h(字节) 和 df -i(inode) 都看！                 │
│   字节满 → du -sh *|sort -h 揪大户; lsof|grep deleted 找占用 │
│   inode满 → 海量小文件; df 显示根盘=挂载没成功写错盘         │
├─────────────────────────────────────────────────────────────┤
│  加组没生效:  id 看新组 → 没有就【重登】; 查命令漏没漏 -aG 的a│
├─────────────────────────────────────────────────────────────┤
│  删了空间没回来:  lsof|grep deleted → 重启占用进程 或 :>截断  │
├─────────────────────────────────────────────────────────────┤
│  心法: 先定位"谁·哪块地·缺哪位权利", 别一上来就 sudo        │
└─────────────────────────────────────────────────────────────┘
```

---

## 10. 三个心法（道）

**心法一：别一上来就 `sudo`。** `sudo` 能"解决"问题，但它把所有权限故障都用大锤砸平，你永远学不会真正的病因，还可能把文件属主改乱（`sudo` 创建的文件归 root，下次你自己又读不了）。先用三个追问定位，需要 root 时也精准知道为什么。

**心法二：删/建看目录，读/改看文件。** 一半的"权限玄学"源于混淆这两者——"我的文件为什么删不掉""空目录为什么建不了文件"，答案都在**目录**的 w，与文件本身无关。

**心法三：报错信息先归类，再动手。** `Permission denied`（权利）、`No space left`（地·字节）、`No space` 但 `df -h` 没满（地·inode）、`Read-only file system`（挂载/盘故障）、加组不认（身份没重载）——**每种错误词对应一棵确定的决策树**。先认词、再走树，比瞎试快十倍。

---

*这份指南是"Linux 服务器管理"的诊断篇：原理（笔记 00–05）是静态结构，排查是逆向诊断。内核同一句——机器里只有「人、地、权利」，任何故障都是"某进程对某块地缺某位权利"。真遇到问题，照第 9 节速查卡跑一遍，5 分钟定位。*
