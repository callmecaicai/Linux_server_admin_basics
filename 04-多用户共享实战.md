# 第 04 章 · 多用户共享实战

> **一句话本质**：多人共享一台机的三大难题——**借权（sudo）、共享一块地（共享目录 + setgid）、防互删（sticky bit）**——全是前三章规则的组合应用，没有新原理。

## 📍 这一章在哪
- 前情：01 管人、02 管权利、03 管地，都是"单人视角"。
- 本章：把多人放进同一台机、同一块目录，看规则怎么协同。
- 贯穿：用到 01 的组、02 的 rwx 与特殊位、03 的归属。

---

## 1. sudo：是"换人"，不是"把我的权限调高"

你对 `/usr` 没有 w，但 `sudo pip install` 能写进去。关键认知：

> **`sudo` 的本质 = "临时以 root 的身份去执行这一条命令"。不是把 `alice` 的权限调高，而是换 `root` 这个人来干。**

所以它能写 `/usr`——因为**执行者变成了 root**，而 root 对一切有权。命令跑完，你立刻变回普通的 `alice`。

- **谁能 sudo**：由 `/etc/sudoers`（用 `visudo` 改）+ 是否在 `sudo`/`wheel` 组决定。`id` 里看到 `sudo` 组就大概有资格。
- **最小化原则**：能不用 sudo 就不用（比如装包用 conda，见第 03 章），真正需要动系统的事才借 root 的手。
- ⚠️ `sudo` 干的事以 root 身份留痕、影响全机，三思再敲。

---

## 2. 共享目录：光 `chmod 770` 不够，还要 setgid

让 `alice / bob / carol` 共同读写一个目录 `/data/proj`：

```bash
sudo groupadd ml                 # 建组（若没有）
sudo usermod -aG ml alice        # 把三人拉进 ml 组（-aG = 追加，别漏 a）
sudo usermod -aG ml bob
sudo usermod -aG ml carol
sudo chgrp ml /data/proj         # 目录归到 ml 组
sudo chmod 2770 /data/proj       # 本组可读写进、外人挡死，且打开 setgid（前面的 2）
```

**为什么是 `2770` 而不是 `770`？** 那个前缀 `2` 是 **setgid**，解决一个隐蔽的坑：

> 没有 setgid 时，bob 在共享目录里**新建的文件，组是 bob 自己的主组**，而不是 `ml` → alice、carol 不在那个组 → **互相读写不了**，协作断在这里。
>
> **setgid 让"目录里新建的文件自动继承目录的组（`ml`）"** → 谁建的文件都归 `ml` 组 → 组员互通。这是共享目录的关键补丁。

> 小提醒一：成员被加进新组后，要**重新登录**（或 `newgrp ml`）才在新会话里生效——`id` 看不到新组多半是这个原因。
>
> 小提醒二：setgid 只对**之后新建**的文件生效。如果 `/data/proj` **已经有旧文件**，它们的组不会被追溯——再补一条 `sudo chgrp -R ml /data/proj` 把存量文件的组也改过来，否则会出现"部分文件组员仍互通不了"。

---

## 3. sticky bit：/tmp 为什么人人可写却删不掉别人的

回扣第 02 章："删文件看的是**目录**的 w"。`/tmp` 对所有人开放 w（人人可写），按这规则任何人都能删别人的文件——显然不行。补丁是 **sticky bit**：

```bash
sudo chmod 1777 /data/shared    # 1=sticky 位；这里设成人人可写的公共目录
ls -ld /data/shared             # drwxrwxrwt  ← 末位的 t 就是 sticky
```

> 加了 sticky bit，规则收紧为："这个目录里，**你只能删/改名自己当属主的文件**（目录属主和 root 不受此限）。" 于是"人人可写"与"防互删"两全。`/tmp` 出厂就带它（权限正是 `1777`）。
>
> 细节：末位 `t` 是小写还是大写有讲究——other 有 x 时显示小写 `t`，没有 x 时显示大写 `T`。所以只 `chmod +t` 而不设 other 的 x，看到的会是 `T`；上面直接用 `1777` 保证 other 有 x，显示小写 `t`。

---

## 4. 特殊权限位三件套（在 rwx 之前再加一位）

| 位 | 数字 | 作用 | 典型 |
|---|---|---|---|
| **setuid** | 4 | 执行该程序时临时变成**属主**身份 | `passwd` 命令（普通用户改密码要写系统文件）|
| **setgid** | 2 | 目录内新文件**继承目录组** | 共享目录 |
| **sticky** | 1 | 目录内**只能删自己的** | `/tmp` |

> 写法：`chmod 2770`（setgid）、`chmod 1777`（sticky）、`chmod 4755`（setuid）——最前面多出来那一位就是它们。

---

## 🔗 实战要点
- 共享目录三件套配方：`groupadd` → `usermod -aG` 拉人 → `chgrp` + `chmod 2770`（带 setgid）。
- 组员"明明加了组却没权限"：99% 是**没重新登录**，或者**目录漏了 setgid**。
- `sudo` 不是万能钥匙，是"借 root 的身份"；能用 conda/家目录解决的就别 sudo。

## 🧠 速记卡
- **sudo = 换人（临时变 root）**，不是提权；资格看 `/etc/sudoers` + sudo 组。
- **共享目录 = 建组 + 拉人 + `chmod 2770`**；`setgid(2)` 让新文件继承组（关键）。
- **sticky bit(`t`)**：人人可写但只能删自己的（`/tmp`）。
- 特殊三件套：setuid 4 / setgid 2 / sticky 1，加在 rwx 最前面。

## ✅ 自检
1. `sudo` 是"提权"还是"换人"？它凭什么能写 `/usr`？
2. 建一个三人可读写的共享目录，为什么 `chmod 770` 不够、还要 `2770`？
3. `/tmp` 怎么做到"人人可写、但删不掉别人的"？

## ➡️ 下一章
多人协作搞定后，回到物理层：好几块盘怎么分工、怎么挂载、怎么 `fstab` 开机自动挂载、要不要 RAID/LVM？下一章《多盘管理与规划》。
