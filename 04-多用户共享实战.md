# 第 04 章 · 多用户共享实战

> **一句话本质**：多人共享一台机的三大难题——**借权（sudo）、共享一块地（共享目录）、防互删（sticky bit）**——全是前三章规则的组合应用。但"共享目录"的正确配方比多数教程讲的多一味：**setgid 解决组、umask/默认 ACL 解决写权限**，两个都到位才真协作。

## 📍 这一章在哪
- 前情：01 管人、02 管权利、03 管地，都是"单人视角"。
- 本章：把多人放进同一台机、同一块目录，看规则怎么协同，并补全共享目录的完整配方。
- 贯穿：用到 01 的组与"加组需重登"、02 的 rwx/umask/ACL、03 的归属与 setgid 例外。

---

## 1. sudo：是"换人"，不是"把我的权限调高"

你对 `/usr` 没有 w，但 `sudo pip install` 能写进去。关键认知：

> **`sudo` 的本质 = "临时以 root 的身份去执行这一条命令"。不是把 `alice` 的权限调高，而是换 `root` 这个人来干。**（接第 00 章：精确说是"让这条进程顶着 UID 0 跑"。）

所以它能写 `/usr`——因为**执行者变成了 root**，而内核对 root 跳过 rwx 检查。命令跑完，你立刻变回普通的 `alice`。

- **谁能 sudo**：由 `/etc/sudoers`（用 `visudo` 改）+ 是否在 `sudo`/`wheel` 组决定。`id` 里看到 `sudo` 组就大概有资格。
- **最小化原则**：能不用 sudo 就不用（比如装包用 conda，见第 03 章），真正需要动系统的事才借 root 的手。
- ⚠️ `sudo` 干的事以 root 身份留痕（记到 `/var/log/auth.log` 或 `journalctl`）、影响全机，三思再敲。

**`sudoers` 不是只能"全给或全不给"——它能精确到命令**（这才是它的精髓）：

```bash
# 用 visudo 编辑（visudo 会做语法检查，防止写错锁死 sudo）
# 语法：  谁    哪台机=(以谁的身份)  能跑哪些命令
alice   ALL=(ALL:ALL)        ALL                      # alice 全权 sudo（要输自己密码）
deploy  ALL=(ALL)  NOPASSWD: /usr/bin/systemctl restart nginx   # 只能免密重启 nginx，别的不行
%ml     ALL=(ALL)  /usr/bin/docker                    # %开头=组：ml 组都能 sudo docker
```

- `%ml` 的 `%` 表示"这是一个**组**"。
- `NOPASSWD:` 让该条免输密码（给自动化/部署脚本用，但范围要卡死到具体命令）。
- **最小权限范例**：给部署账号只开"重启某服务"，而不是 `ALL`——它就算被攻破也只能重启 nginx，干不了别的。

---

## 2. 共享目录：完整配方（光 setgid 还不够，多数教程漏了 umask 这一味）

让 `alice / bob / carol` **都能在 `/data/proj` 里互相读写彼此新建的文件**。完整配方分三步，缺一不可：

### 步骤 ① 建组 + 拉人

```bash
sudo groupadd ml                 # 建组（若没有）
sudo usermod -aG ml alice        # 把三人拉进 ml 组（-aG = 追加，别漏 a！见第 01 章）
sudo usermod -aG ml bob
sudo usermod -aG ml carol
# ⚠️ 三人都要重新登录，id 里才看得到 ml（第 01 章：加组需重登）
```

### 步骤 ② 目录归组 + setgid（解决"新文件的**组**"）

```bash
sudo chgrp ml /data/proj         # 目录归到 ml 组
sudo chmod 2770 /data/proj       # 本组可读写进、外人挡死，且打开 setgid（前面的 2）
sudo chgrp -R ml /data/proj      # 存量旧文件的组也一并改过来（setgid 只管之后新建的）
```

**为什么是 `2770` 而不是 `770`？** 那个前缀 `2` 是 **setgid**：

> 没有 setgid 时，bob 在共享目录里**新建的文件，组是 bob 自己的主组**，而不是 `ml` → alice、carol 不在那个组 → 互相读写不了。
> **setgid 让"目录里新建的文件自动继承目录的组（`ml`）"**（呼应第 03 章"创建者即属主"的唯一例外）→ 谁建的文件都归 `ml` 组。

### 步骤 ③ 解决"新文件的**写位**"——umask 或默认 ACL（最常被漏！）

setgid 让新文件**归了 `ml` 组**，但组成员能不能写，还要看那文件**对组开没开 w**。而新文件的默认权限由 **umask** 决定：

```
默认 umask=022 → 新文件 644 (rw-r--r--) → 组只有 r、没有 w
       ↓ 结果
bob 建的文件归了 ml 组，alice 却只能读不能改 ——"配了 setgid 还是不能协作"的真凶！
```

**两种正解，挑一个：**

**方案 A：放宽 umask（让新文件默认带组写位）**
```bash
# 让在此协作的人 umask 设为 002 → 新文件 664、新目录 775（组带 w）
umask 002          # 写进 ~/.bashrc 长期生效；或在项目的进入脚本里设
```

**方案 B：默认 ACL（更省心、不依赖每个人改 umask，推荐）**
```bash
# 给目录设默认 ACL：此后目录内新建的一切，ml 组自动有 rwx
sudo setfacl -R  -m  g:ml:rwx        /data/proj   # 存量也设上
sudo setfacl -R  -d  -m  g:ml:rwx    /data/proj   # -d = 默认 ACL，管"将来新建的"
```
> 方案 B 的好处：**不靠组员各自记得改 umask**，目录自己保证"新东西 ml 组就能写"。多人协作首选。设完 `getfacl /data/proj` 能看到 `default:group:ml:rwx`。

> **一句话记牢三步**：建组拉人 → `chmod 2770`（setgid 管组）→ umask 002 或默认 ACL（管写位）。**只做前两步是最常见的"假共享"**：组对了、却因为 umask 把组的 w 挖掉了，新文件组员还是改不了。

---

## 3. sticky bit：/tmp 为什么人人可写却删不掉别人的

回扣第 02 章："删文件看的是**目录**的 w"。`/tmp` 对所有人开放 w（人人可写），按这规则任何人都能删别人的文件——显然不行。补丁是 **sticky bit**：

```bash
sudo chmod 1777 /data/shared    # 1=sticky 位；这里设成人人可写的公共目录
ls -ld /data/shared             # drwxrwxrwt  ← 末位的 t 就是 sticky
```

> 加了 sticky bit，规则收紧为："这个目录里，**你只能删/改名自己当属主的文件**（目录属主和 root 不受此限）。" 于是"人人可写"与"防互删"两全。`/tmp` 出厂就带它（权限正是 `1777`）。
>
> 细节：末位 `t` 大小写有讲究——other 有 x 时显示小写 `t`，没有 x 时显示大写 `T`。所以只 `chmod +t` 而不设 other 的 x，看到的会是 `T`；上面直接用 `1777` 保证 other 有 x，显示小写 `t`。

---

## 4. 特殊权限位三件套（在 rwx 之前再加一位）

| 位 | 数字 | 贴在哪 | 作用 | 典型 |
|---|---|---|---|---|
| **setuid** | 4 | 可执行文件 | 运行时进程临时变成**文件属主**身份 | `passwd`（普通用户改密码要写 root 的 `/etc/shadow`）|
| **setgid** | 2 | 目录（也可文件）| 目录内新文件**继承目录组** | 共享目录 |
| **sticky** | 1 | 目录 | 目录内**只能删自己的** | `/tmp` |

```bash
chmod 4755 /usr/bin/xxx    # setuid：rwsr-xr-x（owner 的 x 位变 s）
chmod 2770 /data/proj      # setgid：rwxrws---（group 的 x 位变 s）
chmod 1777 /tmp            # sticky： rwxrwxrwt（other 的 x 位变 t）
```

> 看 `ls -l` 时，特殊位"借住"在某档的 x 位上显示：setuid→owner 的 x 变 `s`、setgid→group 的 x 变 `s`、sticky→other 的 x 变 `t`（大写表示那档原本没 x）。

> ⚠️ **setuid 是双刃剑**：一个属主是 root 的 setuid 程序，任何人运行它的瞬间都"变身 root"。所以它是入侵者最爱的提权点——`find / -perm -4000 -type f 2>/dev/null` 能列出全机所有 setuid 程序，安全审计必查项。自己别随便给脚本加 setuid（而且 Linux 对脚本的 setuid 直接忽略，只对二进制有效）。

---

## 🔗 实战要点
- 共享目录完整配方：`groupadd` → `usermod -aG` 拉人（**重登**）→ `chgrp -R` + `chmod 2770`（setgid）→ **默认 ACL 或 umask 002**（补写位）。
- 组员"明明加了组却没权限"排查顺序：① 重登了吗（`id` 看到组没）② 目录有 setgid 吗（`ls -ld` 看 `s`）③ 新文件组带 w 吗（`ls -l` 看 group 位 / 查 umask / 查默认 ACL）。
- `sudo` 配置走 `visudo`（自带语法检查），权限卡到**具体命令**而非 `ALL`。
- 审计 setuid：`find / -perm -4000 -type f 2>/dev/null` 定期扫。

## 🧠 速记卡
- **sudo = 换人（临时变 root 跑这条命令）**，不是提权；资格看 `/etc/sudoers`（`visudo` 改，可精确到命令/组）+ sudo 组。
- **共享目录 = 建组拉人(重登) + `chmod 2770`(setgid 管组) + umask 002 或默认 ACL(管写位)**；只做前两步=假共享。
- **sticky bit(`t`)**：人人可写但只能删自己的（`/tmp` 的 `1777`）。
- 特殊三件套：setuid 4 / setgid 2 / sticky 1，加在 rwx 最前；显示借住 x 位（`s`/`s`/`t`）。
- setuid 是提权点，`find / -perm -4000` 必查；脚本加 setuid 无效。

## ✅ 自检
1. `sudo` 是"提权"还是"换人"？它凭什么能写 `/usr`？怎么只给某账号开"免密重启 nginx"？
2. 建一个三人可读写的共享目录，为什么 `chmod 2770` 还不够？卡在哪个机制上、怎么补？
3. 配了组、配了 setgid，bob 建的文件 alice 还是改不了，最可能是什么原因？两种修法？
4. `/tmp` 怎么做到"人人可写、但删不掉别人的"？`t` 大小写差在哪？
5. setuid 为什么是安全审计的重点？怎么列出全机 setuid 程序？

## ➡️ 下一章
多人协作搞定后，回到物理层：好几块盘怎么分工、怎么挂载、怎么 `fstab` 开机自动挂载、要不要 RAID/LVM？"`rm` 了大文件磁盘却没空出来"又是怎么回事？下一章《多盘管理与规划》。
