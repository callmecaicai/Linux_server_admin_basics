# 第 02 章 · 权限 rwx

> **一句话本质**：`ls -l` 那 9 个字符 = **三档人（owner/group/other）× 三槽位（rwx）**；数字记法 **r=4 w=2 x=1**；同样的 `rwx` 贴到**目录**上含义完全不同；而内核检查时**对号入座、命中即停、不退档**。

## 📍 这一章在哪
- 前情：第 01 章机器认得你、知道你的 UID/GID 和全部组。
- 本章：把"你的进程对某样东西能干什么"讲死，并补上 umask（新文件默认权限从哪来）和 ACL（超越三档的精细授权）。
- 贯穿：第 03/04 章的目录归属、共享协作全靠这套规则。

---

## 1. 9 个字符 = 三档 × 三槽位

```
-  rw-   r--   r--    model.pt
↑   ↑     ↑     ↑
类型 owner group other
    (属主) (同组) (其他所有人)
```

后 9 个字符**不是一坨，是三组、每组 3 个固定槽位**，顺序永远 `r` `w` `x`：

```
 rw-      r--      r--
└owner┘  └group┘  └other┘
 给属主    给属组    给「其他所有人」
```

---

## 2. 内核怎么判：对号入座、命中即停、**不退档**（关键心智）

很多人以为权限是"把三档加起来看"，错。内核的判定是一条**短路**逻辑：

```
进程的 UID == 文件属主?  ── 是 ──▶ 只用 owner 那 3 位判，结束（不再看 group/other）
        │否
进程的某个组 == 文件属组? ── 是 ──▶ 只用 group 那 3 位判，结束
        │否
                          ─────▶ 用 other 那 3 位判
```

**反直觉但真实的后果：**

> 文件 `----rwxrwx  alice  ml`（owner 档是 `---`）。alice 去读它 → 命中 owner 档 → owner 是 `---` → **拒绝**。哪怕 group/other 全开，**内核不会"退一档"给她**。
> 结论：**owner 档既是权利也是上限**。"我是主人却读不了自己的文件"就是这么来的（少见但真实，面试爱问）。

> 唯一的超级例外是 **root**：内核对 root 进程跳过 rwx 检查（执行除外，至少要有一档带 `x`）。

---

## 3. 数字记法：r=4 w=2 x=1

```
r = 4    w = 2    x = 1    无 = 0   （一格之内，有的相加）
rw- = 4+2 = 6     r-- = 4 = 4     rwx = 7     --- = 0
```

整行 `-rw-r--r--`（rw-/r--/r--）= **644**。常见值对照：

| 数字 | 展开 | 人话 | 典型用途 |
|---|---|---|---|
| `644` | rw- r-- r-- | 我读写，所有人只读 | 普通文件默认（⚠️隐私坑）|
| `640` | rw- r-- --- | 我读写、同组只读、外人全挡 | 收紧的共享文件 |
| `600` | rw- --- --- | 只有我能碰 | **私钥/密钥铁律** |
| `700` | rwx --- --- | 只有我能进/读/改 | 私密目录（如 `~/.ssh`）|
| `750` | rwx r-x --- | 我全权、同组可进可读、外人挡死 | 共享给本组的**目录** |
| `755` | rwx r-x r-x | 我全权、别人可读可执行 | 程序、公开目录 |

> **隐私命门**：新建文件默认常是 `644` → **other 有 r → 全机器任何人都能读你的文件**。多用户机器上"东西被人看光"，多半栽在 other 这一位。保护方向：从 `644/755` 收紧到 `640/750/600`。**但默认值不是天注定的——它由 umask 决定，见第 6 节。**

---

## 4. 改权限：`chmod`（change mode）

```bash
# ① 数字法（主力，一把设全三档）
chmod 600 ~/.ssh/id_rsa     # 私钥：只有我能碰          [属主即你，无需 sudo]
chmod 750 /data/proj        # 目录：我全权、同组可进、外人挡

# ② 符号法（只动某一格时顺手，不影响其他档）
chmod o-r model.pt          # other 去掉 r
chmod g+w model.pt          # group 加上 w
chmod u+x run.sh            # 给属主加执行权，脚本才能跑
chmod a+r public.txt        # a=all，所有档加 r

# ③ 递归 + 区分文件/目录（重要技巧）
chmod -R u+rwX /data/proj   # 大写 X：只给"目录"和"本来就可执行的文件"加 x
                            # → 目录能进、但不会把普通数据文件误标成可执行
```

> 符号：`u`属主 `g`组 `o`其他 `a`全部；`+`加 `-`减 `=`设定（覆盖该档全部三位）。
> **`-R` 配大写 `X` 是黄金组合**：`chmod -R 755` 会把数据文件也加上执行位（不对），而 `chmod -R u=rwX,go=rX` 只给目录和已有可执行文件加 x，数据文件保持不可执行。

---

## 5. 改属主/属组：`chown` / `chgrp`

```bash
sudo chown bob model.pt          # 改属主为 bob          [需 root]
sudo chown bob:ml model.pt       # 同时改属主和属组       [需 root]
chgrp ml model.pt                # 只改属组（你必须是 ml 组成员才行）
sudo chown -R alice:ml /data/proj # 递归改整棵目录的归属
```

| 命令 | 改什么 | 需 root? |
|---|---|---|
| `chmod` | **权限**（rwx 9 位）| 改自己的文件不需要 |
| `chown` | **属主**（甚至连属组）| **是**（送出归属属高危，普通用户不能把文件"送给"别人，否则可绕过磁盘配额）|
| `chgrp` | **属组** | 属主**可**把组改成自己**所属的**组，不需 root |

---

## 6. umask：新文件默认权限从哪来（之前缺的一块）

第 3 节说"默认常是 644"——它**不是写死的**，而是用一个"基准"减去 **umask（权限掩码）** 算出来的：

```
            文件基准 666 (rw-rw-rw-)        目录基准 777 (rwxrwxrwx)
减去 umask        - 022                            - 022
            ─────────────────              ─────────────────
得到默认       644 (rw-r--r--)               755 (rwxr-xr-x)
```

- umask 是"**要挖掉的权限位**"。`022` = 挖掉"组的 w"和"其他的 w" → 别人默认不能写你新建的东西。
- **文件基准是 666 不是 777**：Linux 故意不让新建的**普通文件**默认带执行位（安全：下载/生成的文件不该天生可执行）。目录基准才是 777。
- 想让新文件默认更私密？把 umask 调严：

```bash
umask                # 看当前值（常见 022 或 002）
umask 077            # 此后新建：文件 600、目录 700（只有自己能碰）
# 永久生效：写进 ~/.bashrc 或 ~/.profile
```

> **为什么共享场景常用 `umask 002`？** `002` 只挖"其他人的 w"、**保留组的 w** → 新文件默认 `664`、新目录 `775`，**同组队友天生能改**。配合第 04 章的 setgid 共享目录，这是让协作顺滑的关键一环（多数教程只讲到 setgid 就停了，结果"setgid 给了组、但组成员还是不能写新文件"——根因就是 umask 默认 `022` 把组的 w 挖掉了，第 04 章给完整配方）。

---

## 7. 目录的 rwx ≠ 文件的 rwx（关键陷阱）

同样三个字母，贴到**目录**上含义全变（把目录想成"房间的门 + 一张名册"）：

```
        文件 file                  目录 directory
r  →   读文件内容(cat)       r  →   列出里面有哪些名字(ls 看名册)
w  →   改文件内容            w  →   在里面增/删/改名文件(改名册，需配合 x)
x  →   当程序执行            x  →   进入/穿过(cd、访问里面已知名字的东西)
```

**三个反直觉、但多用户机上真会咬人的推论：**

1. **`r` 和 `x` 能分家**：
   - 有 r 没 x → `ls` 看得到名字、但 `cd` 进不去、里面文件够不着（连 `stat` 都不行）。
   - 有 x 没 r → 进得去、能访问**你已知确切名字**的文件，但 `ls` 被拒（列不出名册）。这正是"知道路径就能用、但列不出目录"的 `750` 之外的玩法。
   - **经典坑**：文件本身 `644` 可读，但所在目录你没 `x` → `cat` 它照样 `Permission denied`——你连门都进不去。

2. **整条路径每一级都要有 x**：访问 `/a/b/c/file`，你需要 `/`、`/a`、`/a/b`、`/a/b/c` **每一级目录都有 x**（穿过权），只要中间断一级，后面全够不着。这是"路径深处文件读不到"的隐形元凶（排查指南有 `namei -l` 一键诊断）。

3. **删文件 = 改目录，跟文件自身权限无关**：能不能删一个文件，看你对它**所在目录**有没有 `w`（+x）。
   - 文件是 `000`、但目录你有 w → 照样删得掉。
   - 文件是你的 `644`、但目录你没 w → 删不掉（报 Permission denied，初学者最懵的一个）。

> **sticky bit（防互删）**：`/tmp` 谁都能写，但又不能让 A 删 B 的文件——给目录加 sticky bit（权限位显示为 `drwxrwxrwt` 末尾的 `t`），规则变成"目录里你只能删自己当属主的文件"。详见第 04 章。

---

## 8. 超越三档：ACL（精细到"单个人"的授权）

三档（owner/group/other）有个硬伤：**只能表达一个属主 + 一个属组**。现实里常有"这个文件，alice 能写、bob 只能读、carol 完全不能碰"——三档凑不出来。补丁是 **ACL（Access Control List，访问控制列表）**：

```bash
getfacl model.pt                       # 看 ACL（ls -l 末尾出现 + 号就代表有 ACL）
setfacl -m u:bob:r-- model.pt          # 给 bob 单独开"只读"
setfacl -m g:audit:r-- model.pt        # 给 audit 组单独开只读
setfacl -x u:bob model.pt              # 移除 bob 的条目
setfacl -b model.pt                    # 清空所有 ACL

# 默认 ACL（default ACL）——给目录设，让"目录里新建的东西自动继承一套权限"
setfacl -m d:g:ml:rwx /data/proj       # d: = default，新建文件自动让 ml 组可读写
```

- `ls -l` 看到权限位末尾有 **`+`**（如 `-rw-rwxr--+`）→ 这文件挂了 ACL，别只看那 9 位，要 `getfacl` 才看全。
- **默认 ACL 是比 setgid 更强的共享方案**：setgid 只解决"新文件的**组**"，默认 ACL 能直接规定"新文件对某组**有 rwx**"，绕过 umask 的干扰（第 04 章会对比）。

> 什么时候才用 ACL？三档不够用时。能用"建个组"解决的就别上 ACL（ACL 多了难审计）；但"临时给某一个人开个权"、"新建文件自动带组权限"这两类需求，ACL 最干净。

---

## 9. 锁文件本身：`chattr`（连 root 都能拦）

rwx 之外还有一层**文件系统属性**，由 `chattr` 设置，连 root 改之前都得先解锁：

```bash
sudo chattr +i important.conf    # +i = immutable，文件被冻结：谁都不能改/删/改名/链接
lsattr important.conf            # 看属性（出现 i 即被锁）
sudo chattr -i important.conf    # 解锁后才能改
sudo chattr +a app.log           # +a = append only，只能追加不能覆盖（防日志被篡改/清空）
```

> 排查时遇到"**我是 root 都删不掉/改不了**，rwx 还是 `rwx`"的灵异事件——十有八九是 `+i`。先 `lsattr` 看一眼。这也是把关键配置/日志"钉死"防手滑或防入侵者篡改的硬手段。

---

## 🔗 实战要点
- 排查"读不到/删不掉"：先 `ls -l 文件`，再 `ls -ld 所在目录`——**两个都要看**（文件权限 + 目录权限），路径深还要 `namei -l 全路径` 查穿过权（排查指南详解）。
- 私钥只要对 **owner 以外**开放（`640`/`644` 都算太松），SSH 客户端就**拒绝加载并报 `permissions are too open`**——私钥永远 `600`、`~/.ssh` 永远 `700`。
- 递归改权限用 **`chmod -R u+rwX`** 而非 `-R 755`，别把数据文件误标成可执行。
- 看到权限位有 `+`（ACL）或改不动疑似锁定（`lsattr` 查 `+i`），别只盯着那 9 位 rwx。

## 🧠 速记卡
- 9 字符 = **owner/group/other × rwx**；数字 **r4 w2 x1**；内核**命中即停不退档**（owner 档是权利也是上限）。
- `644`默认(隐私坑) / `640`/`750`收紧 / `600`私密 / `700`私密目录 / `755`程序目录。
- `chmod` 改权限（数字法 or `u/g/o/a`+`+/-/=`）；`-R u+rwX` 安全递归；`chown` 改属主需 root。
- **umask = 挖掉的权限位**：文件基准 666、目录 777 减 umask；共享场景用 `002`（保留组 w）。
- **目录**：r=看名册、w=增删（配 x）、x=穿过；**删文件靠目录的 w**；**整条路径每级都要 x**；r/x 可分家。
- 三档不够 → **ACL**（`setfacl`，精细到单人；`d:` 默认 ACL 给共享目录）；改不动疑似 **`chattr +i`**（`lsattr` 查）。

## ✅ 自检
1. `-rw-r-----` 翻成数字是多少？念成人话是什么？
2. 文件 `----rwxrwx alice ml`，alice 本人能读吗？为什么？这叫什么规则？
3. 文件 `644` 可读，为什么有时还是 `cat` 不出来？该查哪两样东西？
4. 一个 `000` 的文件，什么情况下你照样能删掉它？删文件到底看谁的权限？
5. 新建文件默认 `644` 是怎么算出来的？想让默认变成 `600`，怎么设？
6. 三个人对一个文件要"一个可写、一个只读、一个禁止"，三档凑不出来，用什么？
7. 你是 root，但一个文件死活删不掉、rwx 看着正常，第一个怀疑什么、敲什么命令？

## ➡️ 下一章
权限是规则，规则得贴在**具体目录**上。整台机器的目录按什么逻辑摆？哪些能玩、哪些碰了出事？为什么 conda 装包免 sudo？下一章《目录架构》。
