# 第 02 章 · 权限 rwx

> **一句话本质**：`ls -l` 那 9 个字符 = **三档人（owner/group/other）× 三槽位（rwx）**；数字记法 **r=4 w=2 x=1**；而同样的 `rwx` 贴到**目录**上含义完全不同。

## 📍 这一章在哪
- 前情：第 01 章机器认得你、知道你的 UID/GID。
- 本章：把"你对某样东西能干什么"讲死。
- 贯穿：第 03/04 章的目录归属、共享协作全靠这套规则。

---

## 1. 9 个字符 = 三档 × 三槽位

```
-  rw-   r--   r--    model.pt
↑   ↑     ↑     ↑
类型 owner group other
    (属主) (同组) (其他所有人)
```

后 9 个字符**不是一坨，是三组、每组 3 个固定槽位**，顺序永远 `r` `w` `x`：

```
 rw-      r--      r--
└owner┘  └group┘  └other┘
 给属主    给同组    给「组外所有人」
```

> 机器对号入座：你是属主→看 owner 那组；同组→看 group 那组；都不是→看 **other** 那组。

---

## 2. 数字记法：r=4 w=2 x=1

```
r = 4    w = 2    x = 1    无 = 0   （一格之内，有的相加）
rw- = 4+2 = 6     r-- = 4 = 4     rwx = 7     --- = 0
```

整行 `-rw-r--r--`（rw-/r--/r--）= **644**。常见值对照：

| 数字 | 展开 | 人话 | 典型用途 |
|---|---|---|---|
| `644` | rw- r-- r-- | 我读写，所有人只读 | 普通文件默认（⚠️隐私坑）|
| `640` | rw- r-- --- | 我读写、同组只读、外人全挡 | 收紧的共享文件 |
| `600` | rw- --- --- | 只有我能碰 | **私钥/密钥铁律** |
| `750` | rwx r-x --- | 我全权、同组可进可读、外人挡死 | 共享给本组的**目录** |
| `755` | rwx r-x r-x | 我全权、别人可读可执行 | 程序、公开目录 |

> **隐私命门**：新建文件默认常是 `644` → **other 有 r → 全机器任何人都能读你的文件**。多用户机器上"东西被人看光"，多半栽在 other 这一位。保护方向：从 `644/755` 收紧到 `640/750/600`。

---

## 3. 改权限：`chmod`（change mode）

```bash
# ① 数字法（主力，一把设全）
chmod 600 ~/.ssh/id_rsa     # 私钥：只有我能碰          [属主即你，无需 sudo]
chmod 750 /data/proj        # 目录：我全权、同组可进、外人挡

# ② 符号法（只动某一格时顺手）
chmod o-r model.pt          # other 去掉 r
chmod g+w model.pt          # group 加上 w
chmod u+x run.sh            # 给属主加执行权，脚本才能跑
```
> 符号：`u`属主 `g`组 `o`其他 `a`全部；`+`加 `-`减 `=`设定。

---

## 4. 权限相关命令全表

| 命令 | 干什么 | 需 root? |
|---|---|---|
| `ls -l` | **看**权限/属主/组 | 否 |
| `chmod` | 改**权限**（rwx 9 位）| 改自己的不需要 |
| `chown` | 改**属主** | **是**（改归属属高危）|
| `chgrp` | 改**组** | 属主可改成自己所属的组 |
| `id` / `groups` | 看我是谁、在哪些组 | 否 |
| `umask` | 看/设**新文件默认权限** | 否 |
| `sudo` | 临时借 root 权干一件事 | 需在 sudo 组 |

> 日常 90%：**`ls -l` 看 + `chmod` 改**。

---

## 5. 目录的 rwx ≠ 文件的 rwx（关键陷阱）

同样三个字母，贴到**目录**上含义全变（把目录想成"房间的门"）：

```
        文件 file                  目录 directory
r  →   读文件内容(cat)       r  →   列出里面有哪些名字(ls)
w  →   改文件内容            w  →   在里面增/删/改名文件
x  →   当程序执行            x  →   进入/穿过(cd、访问里面的东西)
```

**两个反直觉、但多用户机上真会咬人的推论：**

1. **`r` 和 `x` 能分家**：
   - 有 r 没 x → 看得到名字、但 `cd` 进不去、里面文件够不着。
   - 有 x 没 r → 进得去、能访问**你已知名字**的文件，但 `ls` 被拒（列不出清单）。
   - **经典坑**：文件本身 `644` 可读，但所在目录你没 `x` → `cat` 它照样 `Permission denied`——你连门都进不去。

2. **删文件 = 改目录**：能不能删一个文件，看你对它所在**目录**有没有 `w`，与文件自身权限无关。
   - 文件是 `000`、但目录你有 w → 照样删得掉。
   - 文件是你的 `644`、但目录你没 w → 删不掉。

> **sticky bit（防互删）**：`/tmp` 谁都能写，但又不能让 A 删 B 的文件——给目录加 sticky bit（权限位显示为 `drwxrwxrwt` 末尾的 `t`），规则变成"目录里你只能删自己当属主的文件"。详见第 04 章。

---

## 🔗 实战要点
- 排查"读不到/删不掉"：先 `ls -l 文件`，再 `ls -ld 所在目录`——**两个都要看**（文件权限 + 目录权限）。
- 私钥只要对 **owner 以外**开放（`640`/`644` 都算太松），SSH 客户端就**拒绝加载并报 `permissions are too open`**——记住私钥永远 `600`。

## 🧠 速记卡
- 9 字符 = **owner/group/other × rwx**；数字 **r4 w2 x1**。
- `644`默认(隐私坑) / `640`/`750`收紧 / `600`私密 / `755`程序目录。
- `chmod` 改（数字法 or `u/g/o/a` + `+/-/=`）；`chown` 改属主需 root。
- **目录**：r=看名单、w=增删、x=进门；**删文件靠目录的 w**；r 与 x 可分家。

## ✅ 自检
1. `-rw-r-----` 翻成数字是多少？念成人话是什么？
2. 文件 `644` 可读，为什么有时还是 `cat` 不出来？该查什么？
3. 一个 `000` 的文件，什么情况下你照样能删掉它？
4. 私钥该 `chmod` 多少？为什么不能 `644`？

## ➡️ 下一章
权限是规则，规则得贴在**具体目录**上。整台机器的目录按什么逻辑摆？哪些能玩、哪些碰了出事？下一章《目录架构》。
