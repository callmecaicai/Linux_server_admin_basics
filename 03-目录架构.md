# 第 03 章 · 目录架构

> **一句话本质**：Linux 整棵目录树按"谁该往里写东西"分两类——**🔴系统地盘**（出厂摆好、属 root、你只读）和 **🟢数据/人地盘**（给你和数据住、随便折腾）。看懂这刀切，连 conda 为什么免 sudo 都解释了。

## 📍 这一章在哪
- 前情：第 02 章会读权限了，但权限贴在哪些目录上还没讲。
- 本章：给整棵 `/` 目录树画地图，分清"能玩 vs 碰了出事"。
- 贯穿：第 05 章多盘管理把"数据地盘"的物理层接上。

---

## 1. 总钥匙：两类地盘

```
🔴 系统的地盘   出厂摆好、属 root、你只读，动了系统会坏
🟢 数据人地盘   给你和数据住，随便折腾
```

带着这把钥匙看 `/` 下任何目录，先问一句：**这是系统的，还是给数据/人的？**

---

## 2. 目录功能地图

**🔴 系统的地盘（只读，别乱动）**

| 目录 | 干什么 | 类比 |
|---|---|---|
| `/` | 根，一切的起点 | 大楼地基 |
| `/bin` `/usr/bin` | **命令程序本体**：`ls`/`cp`/`python` 这些可执行文件 | 工具间 |
| `/etc` | **系统配置**（`/etc/passwd` 名单就在这）| 总控制室 |
| `/usr` | **装好的软件和库的大本营**（Unix System Resources）| 软件仓库 |
| `/var` | **经常变化的数据**：日志 `/var/log`、缓存 | 流水账本 |
| `/lib` `/boot` | 系统库 / 开机内核 | 发动机舱 |
| `/root` | **root 用户的家**（注意：不是 `/`）| 经理私人办公室 |

> **为什么别乱删**：系统命令、开机程序、别人的服务全靠这些活着。而且这些目录**属主是 root，你是 other、没有 w**——权限本就在拦着你手滑。

**🟢 数据 / 人的地盘（随便折腾）**

| 目录 | 干什么 | 你的用法 |
|---|---|---|
| `/home/alice` | **你的家**，代码、配置、小文件 | 私人房间 |
| `/mnt` `/media` | **挂载点**：把额外硬盘接进目录树（第 05 章 `mount`）| 数据盘入口 |
| `/data` | 习惯性的**数据盘**目录 | 数据集/模型/checkpoint |
| `/tmp` | **临时垃圾场**：人人可写、**重启清空** | 临时解压、中间文件 |
| `/dev/shm` | **内存盘**：极快、断电即失 | 临时高速缓存 |

- **`/mnt` `/data`** = "把一块硬盘接进目录树"的**挂载点**（详见第 05 章）。
- **`/tmp`** = 公共临时区，人人可写，靠 **sticky bit** 保证"你只能删自己的"（第 04 章）。

---

## 3. 实战案例：系统 Python vs conda（把地图用活）

两套 Python 住在**两类地盘**，这一刀解释了 conda 存在的全部意义：

```
系统自带 Python      →  /usr            （🔴系统地盘，属 root）
你装的 conda Python  →  ~/miniconda3 或 /data/alice/miniconda3 （🟢你的地盘，属你）
```

| | 系统 Python（`/usr`）| conda Python（你的地盘）|
|---|---|---|
| 装包要 `sudo`? | **要**（系统地盘你没 w）| **不要**（你的地盘你有 w）|
| 装坏了影响谁? | **整个系统**（系统脚本都靠它）| **只你自己** |
| 多人共用打架? | **会**（一个全局，互相覆盖）| **不会**（人手一套）|
| 多版本切换? | 很难 | **随便**（`conda create` 一项目一套）|

> **conda 的本质 = 把一整套 Python 搬进你自己的地盘。** 一搬进来，第 02 章的权限规则立刻生效：那是你的家、你有 w，所以 `pip install` 想装就装、免 sudo、碰不坏系统、和别人互不干扰。
> **这就是多用户服务器一律推荐人手一个 conda、而不动系统 Python 的根本原因。**

---

## 4. 🔑 铁律：创建者即属主

> **你新创建的任何文件/目录，属主自动就是「创建它的你」，无需任何人授予。**

由此厘清一个常见混淆：
- root 执行 `useradd alice` 时，给你**开了家这块地基**：建 `/home/alice` 并把它的属主设成 `alice`。这一步是 root 干的。
- 但你之后在家里**亲手创建的一切**（conda、代码、文件），属主**自动是你**——**不是 root 一件件赏给你的**，是"创建者即属主"在自动生效。

```
/usr         属主=root，你是 other 无 w  → 装包被拒 → 借 root 的手(sudo)
~/miniconda3 你亲手创建，属主=你，天生有 w → 想装就装，免 sudo
```

---

## 🔗 实战要点
- 进新机器认地形：`ls -l /`（看根下有哪些目录、各自属谁）、`df -hT`（看哪些是挂载的数据盘）。
- **原则（接第 05 章）**：大文件（数据集/模型/checkpoint/conda/缓存）**一律放数据盘** `/data/` 或 `/mnt/`，**别塞进 `/home` 或系统盘**——家目录常在容量较小的系统盘上，塞满会拖垮整机。
- 把 pip/conda/缓存也指到数据盘（如 `PIP_CACHE_DIR`、`HF_HOME`），否则几十 G 的包和模型悄悄塞爆系统盘。

## 🧠 速记卡
- 两类地盘：**🔴系统**（`/etc`/`/usr`/`/bin`/`/var`，属 root、只读）+ **🟢数据人**（`/home`/`/data`/`/mnt`/`/tmp`，给你住）。
- `/mnt` `/data` = 挂载点（接硬盘）；`/tmp` = 公共临时（sticky bit 防互删）。
- **conda 精髓** = 把 Python 搬进你的地盘 → 免 sudo、不坏系统、多人隔离。
- **🔑 创建者即属主**：你建的东西自动归你。
- 大文件放数据盘，别塞系统盘/家目录。

## ✅ 自检
1. `/usr` 和 `/root` 各是什么？为什么你不该往 `/usr` 写东西？
2. 为什么 conda 装包免 sudo、系统 Python 装包要 sudo？卡在哪个权限上？
3. "创建者即属主"是什么意思？root 到底给了你什么、又没给你什么？
4. 几十 G 的数据集该放哪、不该放哪？为什么？

## ➡️ 下一章
单人地形清楚了。多人同时在这些目录上干活，怎么不互相踩？`sudo` 到底是"提权"还是"换人"？共享目录怎么设？下一章《多用户共享实战》。
