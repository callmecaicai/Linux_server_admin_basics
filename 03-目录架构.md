# 第 03 章 · 目录架构

> **一句话本质**：Linux 整棵目录树按"谁该往里写东西"分两类——**🔴系统地盘**（出厂摆好、属 root、你只读）和 **🟢数据/人地盘**（给你和数据住、随便折腾）。这套布局有正式标准（FHS）。看懂这刀切，连 conda 为什么免 sudo 都解释了。

## 📍 这一章在哪
- 前情：第 02 章会读权限了，但权限贴在哪些目录上还没讲。
- 本章：给整棵 `/` 目录树画地图，分清"能玩 vs 碰了出事"，并讲清"名字 vs 数据"（inode 与链接）。
- 贯穿：第 05 章多盘管理把"数据地盘"的物理层接上。

---

## 1. 总钥匙：两类地盘

```
🔴 系统的地盘   出厂摆好、属 root、你只读，动了系统会坏
🟢 数据人地盘   给你和数据住，随便折腾
```

带着这把钥匙看 `/` 下任何目录，先问一句：**这是系统的，还是给数据/人的？**

> 这套目录布局不是某台机器的习惯，而是有正式标准——**FHS（Filesystem Hierarchy Standard，文件系统层次标准）**。所以你在 Ubuntu、CentOS、Debian 上看到的 `/etc`、`/usr`、`/var` 含义都一样，跨发行版通用。

---

## 2. 目录功能地图

**🔴 系统的地盘（只读，别乱动）**

| 目录 | 干什么 | 类比 |
|---|---|---|
| `/` | 根，一切的起点（注意：不是哪个用户的家）| 大楼地基 |
| `/bin` `/usr/bin` | **命令程序本体**：`ls`/`cp`/`python` 这些可执行文件 | 工具间 |
| `/sbin` `/usr/sbin` | **管理员命令**：`fdisk`/`mount`/`useradd`（多数要 root）| 管理员工具间 |
| `/etc` | **系统配置**（`/etc/passwd` 名单、`/etc/fstab` 挂载表都在这）| 总控制室 |
| `/usr` | **装好的软件和库的大本营**（词源见下方📌）| 软件仓库 |
| `/var` | **经常变化的数据**：日志 `/var/log`、缓存、`/var/run` 套接字 | 流水账本 |
| `/lib` `/lib64` | 系统共享库（`.so`，相当于 Windows 的 DLL）| 零件库 |
| `/boot` | 开机内核与引导程序 | 发动机点火系统 |
| `/proc` `/sys` | **虚拟文件系统**：内核/进程的实时状态，不占磁盘（`/proc/cpuinfo` 看 CPU）| 仪表盘（实时读数）|
| `/usr/local` `/opt` | **手动装的软件**：`make install` 默认进 `/usr/local`，整包第三方软件进 `/opt`——都不归发行版包管理器管，但**属 root、写入仍要 sudo** | 自购设备间 |
| `/root` | **root 用户的家**（注意：不是 `/`）| 经理私人办公室 |

> 📌 **词源纠正（原笔记此处有误）**：`/usr` **不是** "Unix System Resources" 的缩写。在最早的 Unix 里，`/usr` 实实在在就是 **"user"（用户）**——当年用户的家目录放在 `/usr/alice`。后来磁盘满了，管理员把系统程序挪进 `/usr`，它才逐渐变成"系统软件仓库"，用户的家则迁去了 `/home`。"Unix System Resources" 是**后人回溯编出来的助记词（backronym）**，记忆无妨，但别当成真正的历史。

> **为什么别乱删**：系统命令、开机程序、别人的服务全靠这些活着。而且这些目录**属主是 root，你是 other、没有 w**——权限本就在拦着你手滑（第 02 章）。

**🟢 数据 / 人的地盘（随便折腾）**

| 目录 | 干什么 | 你的用法 |
|---|---|---|
| `/home/alice` | **你的家**，代码、配置、小文件 | 私人房间（`$HOME`、`~`）|
| `/mnt` `/media` | **挂载点**：把额外硬盘接进目录树（第 05 章 `mount`）| 数据盘入口 |
| `/data` | 习惯性的**数据盘**目录（非 FHS 标准，但业界惯例）| 数据集/模型/checkpoint |
| `/tmp` | **临时垃圾场**：人人可写、**重启清空** | 临时解压、中间文件 |
| `/dev/shm` | **内存盘**（tmpfs）：极快、断电即失 | 临时高速缓存 |

- **`/mnt` `/data`** = "把一块硬盘接进目录树"的**挂载点**（详见第 05 章）。
- **`/tmp`** = 公共临时区，人人可写，靠 **sticky bit** 保证"你只能删自己的"（第 04 章）。
- ⚠️ `/proc`、`/sys`、`/dev/shm` 看着像目录，其实是**内核造出来的虚拟文件系统**，`df -hT` 里类型显示 `proc`/`sysfs`/`tmpfs`，不占物理磁盘。

---

## 3. 名字 vs 数据：inode 与两种链接（之前缺的一块）

要真正理解第 02 章"删文件 = 改目录"，得先看清 Linux 把文件拆成**两半**：

```
目录项（名字）  ───指向──▶  inode（档案：权限/属主/时间/数据块指针）───▶  磁盘上的数据
  model.pt                  #1048576                                  [实际字节]
 （挂在目录里）            （ls -l 看到的一切都在这）
```

- **inode** = 文件的"户口本"：权限、属主、时间戳、大小、**指向数据块的指针**全在这，**唯独没有文件名**。
- **文件名** 只是某个目录里的一个"标签"，指向某个 inode 号。
- 所以"删文件"删的是**目录里那个标签**（所以看目录的 w），inode 要等到**没有任何标签指着它、且没有进程开着它**时才真正释放——这条是第 05 章"`rm` 了大文件磁盘却没空出来"和排查指南里 `lsof | grep deleted` 的根。

**两种链接，正好对应"标签"与"户口本"：**

| | 硬链接 hard link | 软链接 symbolic link |
|---|---|---|
| 建立 | `ln 源 新名` | `ln -s 源 新名` |
| 本质 | 给**同一个 inode** 再起一个名字 | 一个**特殊文件，内容是一段路径**（指路牌）|
| `ls -l` | 看不出区别（第 2 列**硬链接数**会 >1）| 第一个字符是 `l`，显示 `新名 -> 源` |
| 删原名 | 数据还在（另一个名字还指着 inode）| **断链**（指路牌指向的路径没了，变"死链"）|
| 跨文件系统/盘 | **不行**（inode 号只在本文件系统内唯一）| 可以（存的是路径字符串）|
| 指向目录 | 一般不允许 | 可以（`/usr/bin/python -> python3.10` 就是）|

> `ls -l` 第 2 列那个常被忽略的数字，就是**硬链接数**——有几个名字指着这同一个 inode。普通文件通常是 1；目录天生 ≥2（因为目录里的 `.` 也算指向自己的一个链接）。

---

## 4. 实战案例：系统 Python vs conda（把地图用活）

两套 Python 住在**两类地盘**，这一刀解释了 conda 存在的全部意义：

```
系统自带 Python      →  /usr            （🔴系统地盘，属 root）
你装的 conda Python  →  ~/miniconda3 或 /data/alice/miniconda3 （🟢你的地盘，属你）
```

| | 系统 Python（`/usr`）| conda Python（你的地盘）|
|---|---|---|
| 装包要 `sudo`? | **要**（系统地盘你没 w）| **不要**（你的地盘你有 w）|
| 装坏了影响谁? | **整个系统**（系统脚本都靠它）| **只你自己** |
| 多人共用打架? | **会**（一个全局，互相覆盖）| **不会**（人手一套）|
| 多版本切换? | 很难 | **随便**（`conda create` 一项目一套）|

> **conda 的本质 = 把一整套 Python 搬进你自己的地盘。** 一搬进来，第 02 章的权限规则立刻生效：那是你的家、你有 w，所以 `pip install` 想装就装、免 sudo、碰不坏系统、和别人互不干扰。
> **这就是多用户服务器一律推荐人手一个 conda、而不动系统 Python 的根本原因。**

---

## 5. 🔑 铁律：创建者即属主

> **你新创建的任何文件/目录，属主自动就是「创建它的进程的 UID」，无需任何人授予。**（精确表述见第 00 章"权限主语是进程"。）

由此厘清一个常见混淆：
- root 执行 `useradd -m alice` 时，给你**开了家这块地基**：建 `/home/alice` 并把它的属主设成 `alice`。这一步是 root 干的。
- 但你之后在家里**亲手创建的一切**（conda、代码、文件），属主**自动是你**——**不是 root 一件件赏给你的**，是"创建者即属主"在自动生效。

```
/usr         属主=root，你是 other 无 w  → 装包被拒 → 借 root 的手(sudo)
~/miniconda3 你亲手创建，属主=你，天生有 w → 想装就装，免 sudo
```

> 唯一的例外是设了 **setgid 的目录**：在那种目录里新建的文件，**属组**会继承目录的组而不是你的主组（第 04 章共享目录的关键机制）。属主仍然是你。

---

## 🔗 实战要点
- 进新机器认地形：`ls -l /`（看根下有哪些目录、各自属谁）、`df -hT`（看哪些是挂载的数据盘、哪些是 tmpfs/proc 虚拟的）。
- **原则（接第 05 章）**：大文件（数据集/模型/checkpoint/conda/缓存）**一律放数据盘** `/data/` 或 `/mnt/`，**别塞进 `/home` 或系统盘**——家目录常在容量较小的系统盘上，塞满会拖垮整机、甚至导致服务起不来。
- 把 pip/conda/缓存也指到数据盘（环境变量 `PIP_CACHE_DIR`、`HF_HOME`、`CONDA_ENVS_PATH`），否则几十 G 的包和模型悄悄塞爆系统盘。
- 遇到"指向哪去了"的疑惑（命令、库、python）：`readlink -f 路径` 一路解软链接到真身；`which -a python` 看 PATH 里有几个同名。

## 🧠 速记卡
- 两类地盘：**🔴系统**（`/etc`/`/usr`/`/bin`/`/var`/`/boot`，属 root、只读）+ **🟢数据人**（`/home`/`/data`/`/mnt`/`/tmp`，给你住）；标准叫 **FHS**。
- `/usr` 词源是 **"user"**，"Unix System Resources" 是后造助记词；`/proc`/`/sys` 是**虚拟**文件系统不占盘。
- **文件 = 名字（目录项）+ inode（户口本）+ 数据块**；删名字看目录 w，inode 等"无名字+无进程占用"才释放。
- **硬链接**=同 inode 多个名（不跨盘、删一个数据还在）；**软链接**=存路径的指路牌（`l`、可跨盘、可指目录、原文件没了变死链）。
- **conda 精髓** = 把 Python 搬进你的地盘 → 免 sudo、不坏系统、多人隔离。
- **🔑 创建者即属主**（setgid 目录例外：继承组）；大文件放数据盘别塞系统盘/家目录。

## ✅ 自检
1. `/usr` 的真实词源是什么？为什么你不该往 `/usr` 写东西？
2. 文件名存在哪、权限属主存在哪？"删文件 = 改目录"为什么成立？
3. 硬链接和软链接的本质区别？哪个能跨盘、哪个能指目录、删原文件各会怎样？
4. 为什么 conda 装包免 sudo、系统 Python 装包要 sudo？卡在哪个权限上？
5. "创建者即属主"是什么意思？什么目录会破例改掉新文件的属组？
6. `/proc/cpuinfo` 占磁盘空间吗？它是什么？

## ➡️ 下一章
单人地形清楚了。多人同时在这些目录上干活，怎么不互相踩？`sudo` 到底是"提权"还是"换人"？共享目录怎么设才能让组员互相读写（光 setgid 还不够，还卡在 umask）？下一章《多用户共享实战》。
