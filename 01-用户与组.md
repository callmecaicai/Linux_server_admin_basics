# 第 01 章 · 用户与组

> **一句话本质**：机器认人**不靠 IP**（IP 是"你从哪来"，身份是"你是谁"，两层解耦）；它靠几份本地名单（`/etc/passwd`、`/etc/shadow`、`/etc/group`），内部用**数字身份证号 UID/GID**（不是名字、更不是指针）记账。而且"用户"不止是活人——一半以上的账号是给**程序**用的。

## 📍 这一章在哪
- 前情：第 00 章说"管人"是三样东西之首，并点出权限的主语其实是"顶着 UID 的进程"。
- 本章：钻进"机器怎么认识一个人/一个程序、怎么把他们分队"。
- 贯穿：UID/GID 是第 02 章权限"对号入座"的依据；系统用户是第 03 章"服务各自隔离"的基础。

---

## 1. 机器认人不靠 IP——身份与来源解耦

登录一台机器其实是**两层、两件事**：

| | 回答 | 谁管 |
|---|---|---|
| **网络层** | 你**从哪来**（IP/路由/堡垒机）| 网络 |
| **系统层**（本章）| 你**是谁**（用户名 + 密钥）| 操作系统 |

> **证据**：同一个账号 `alice`，从家里宽带、公司 WiFi、手机热点登进去——IP 每次都不同，但机器每次都认你是 `alice`，进去都是同一个家目录、同一批文件。
> 若机器靠 IP 认人，换个网你就成陌生人了——显然不是。**机器的住户名单里，一个字节的 IP 都不存。**

---

## 2. 住户名单：`/etc/passwd`

机器认得谁，写在一个**纯文本文件** `/etc/passwd` 里，一行一个住户。真实一行长这样——**字段用冒号分隔、之间没有空格**：

```
alice:x:1001:1001:Alice Chen,,,:/home/alice:/bin/bash
```

拆成 7 个字段看：

| 列 | 字段 | 例值 | 含义 |
|---|---|---|---|
| ① | 用户名 | `alice` | 给人看的名字 |
| ② | 密码占位 | `x` | `x` 表示"真密码在 `/etc/shadow`"；若这里是空 = **免密码登录**（危险）|
| ③ | **UID** | `1001` | **你的身份证号**（核心）|
| ④ | **GID** | `1001` | 你**主组**的编号 |
| ⑤ | 备注(GECOS) | `Alice Chen,,,` | 全名/办公室/电话，逗号分隔；常见 `,,,` 是全空 |
| ⑥ | **家目录** | `/home/alice` | 登录后落脚处（`$HOME`、`cd` 不带参数去的地方）|
| ⑦ | 默认 shell | `/bin/bash` | 给你哪个"壳"；**`/usr/sbin/nologin` 或 `/bin/false` = 禁止登录**（见第 4 节）|

> **密码到底在哪？** 真正的密码哈希在 `/etc/shadow`（权限 `640`、属组 `shadow`，**普通用户根本读不了**——`cat` 它会 Permission denied）。把哈希从人人可读的 `passwd` 拆到只有特权能读的 `shadow`，正是"最小可见"的安全设计。SSH 公钥则在你家目录的 `~/.ssh/authorized_keys`。

**`/etc/shadow` 一行长这样**（了解即可，能看懂关键字段）：

```
alice:$6$xyz...$hash...:19700:0:99999:7:::
       └─────┬──────┘  └─最后改密码的天数 / 最短 / 最长有效期 / 警告期─┘
        密码哈希（$6$=SHA-512）
```
- 哈希前缀标算法：`$6$`=SHA-512（现代默认）、`$y$`=yescrypt（更新）、`$1$`=MD5（老旧别用）。
- 字段是 `!` 或 `*` 开头 = **该账号被锁定/不能用密码登录**（系统账号常见）。

---

## 3. UID = 身份证号（不是名字，更不是指针）

机器内部**不用 `alice` 这个名字**记账，用数字 `1001`。

> **UID 不是指针、不指向任何物理位置。它就是一个"身份证号/学号"——纯标识符。**

**为什么用数字而不用名字？两个真实原因：**

1. **改名解耦（最关键）**：每个文件的档案（inode）里盖的是**号码 1001**，不是名字。哪天 `alice` 改名 `alex`，只改名单里一行，号码 1001 不变 → 全盘文件自动还是你的。若盖的是名字，改名要扫全盘，漏一个就"不认你"。
2. **定长又快**：号码固定几字节；名字是变长字符串，占地、比较慢。

**于是有一张"翻译表"：**
```
磁盘上文件只盖号码：           属主 = 1001
                                  ↕  （需要时查表）
/etc/passwd 是「号码↔名字」表：  1001 ⟷ alice
```
- 机器**内部记账/判归属**：永远用号码（快、稳、不怕改名）。
- 给**你看**（`ls -l`）：临时拿号码去查表，翻成 `alice` 显示。

**孤儿文件**：若某文件属主是 `1005`、而 `1005` 用户被删（表里没这行了），`ls -l` 翻不出名字，就露出光秃秃的号码：
```
-rw-r--r--  1  1005  1005  2048  old_data.bin
```
> 这反过来证明：文件上盖的从来是**号码**，名字是临时查表翻的，表没了只剩号码。删用户时若不连家目录一起清，这些孤儿文件还可能被**新建的、恰好复用了 1005 的用户**意外接管——所以 `userdel` 要不要带 `-r`、UID 要不要复用，是真实的运维考量。

---

## 4. UID 不只给活人——三类账号与 UID 区间

打开 `/etc/passwd` 你会吓一跳：几十行，但活人就一两个。其余都是**给程序用的账号**。系统靠 **UID 区间**把它们分成三类（界线写在 `/etc/login.defs`，下面是常见默认）：

| 类别 | UID 区间 | 是谁 | 能登录吗 |
|---|---|---|---|
| **root** | `0` | 超级管理员，规则的超级例外 | 能（生产上常禁止直接 ssh root）|
| **系统/服务账号** | `1`–`999` | `nginx`、`mysql`、`www-data`、`sshd`、`docker` 等**给守护进程用** | **不能**（shell 是 `nologin`）|
| **普通用户** | `1000`+ | 真人 `alice`/`bob`（第一个建的人通常是 1000）| 能 |

**为什么程序也要有"用户"？——最小权限隔离。**

> nginx 用专属账号 `www-data`（UID 33）跑。万一 nginx 被黑，攻击者拿到的只是 `www-data` 的权利——读不了 `/etc/shadow`、动不了别的服务的数据、删不掉系统文件。**每个服务一个低权账号，把"被攻破"的爆炸半径压到最小。** 这正是第 00 章"权限主语是进程"的现实意义：进程顶着 `www-data` 跑，权限就被框死在 `www-data` 能碰的那一小块。

所以看到 `/etc/passwd` 里一堆 `nologin` 账号别慌——那是健康的、各司其职的服务身份。

---

## 5. 组（group）= 把人分队

只讲"单个住户"不够，多人共享要把人**分队**——这就是组（GID）。

| | 是什么 | 比喻 | 号码 |
|---|---|---|---|
| **用户 user** | 单个住户 | 你这个人 | UID |
| **组 group** | 一队住户 | 你的部门/班级 | GID |

> **为什么要组？** 权限常常是给**一队人**、而非一个人。
> 例：你们仨 `alice / bob / carol` 共享数据集 `/data/datasets`，想让仨人都能读、外人不能碰。不必逐人授权——建个组 `ml`，把仨人拉进去，对目录说一句"本组可读"即可。以后来新人，拉进 `ml` 组自动有权限。

**主组 vs 附加组（一个关键区分）：**

| | 哪来的 | 有几个 | 作用 |
|---|---|---|---|
| **主组（primary）** | `/etc/passwd` 第 4 列那个 GID | **正好 1 个** | 你**新建文件**时，文件的属组默认就是它 |
| **附加组（supplementary）** | `/etc/group` 里把你列进去的组 | **0 到多个** | 给你额外的"队员资格"，用于访问别人/共享的资源 |

```
id 的输出一眼看全：
uid=1001(alice) gid=1001(alice) groups=1001(alice),27(sudo),999(docker),1500(ml)
                 └──主组──┘      └──────────全部组（含主组+附加组）──────────┘
```
- 看到 `sudo`（或某些发行版叫 `wheel`）→ 你有借 root 的资格（第 04 章）。
- 看到 `docker` → 你能操作 docker（≈ 等价 root，因为能挂载宿主任意目录，安全上要当回事）。
- 组的名单在 `/etc/group`，一行 `组名:x:GID:成员1,成员2`。

> ⚠️ **加组不即时生效**：`usermod -aG ml alice` 之后，alice **当前已登录的会话里 `id` 还看不到 `ml`**——组成员关系是在**登录那一刻**载入进程的。必须**重新登录**（或对新开的子shell用 `newgrp ml`）才生效。这是"明明加了组却没权限"的头号原因（第 04 章实战会再撞上）。

---

## 6. 用户/组管理命令全表（多数需 root）

| 命令 | 干什么 | 需 root? | 备注 |
|---|---|---|---|
| `whoami` | 我是谁（用户名）| 否 | 只读 |
| `id` | 我的 UID/GID + 全部组 | 否 | **最有用**，进新机器先敲它 |
| `groups` | 我属于哪些组 | 否 | `id` 的精简版 |
| `getent passwd alice` | 查某用户（含 LDAP/网络用户，比 `cat /etc/passwd` 全）| 否 | 大型环境用户可能不在本地文件里 |
| `useradd` / `adduser` | 建用户（`adduser` 是更友好的交互式封装）| **是** | `useradd -m -s /bin/bash alice` 建家目录+给shell |
| `userdel -r alice` | 删用户（`-r` 连家目录一起删）| **是** | 不带 `-r` 会留下孤儿文件 |
| `usermod -aG ml alice` | 把人加进附加组（**`-aG` 的 `a`=追加，漏了 `a` 会清空原有附加组！**）| **是** | 高频踩坑点 |
| `passwd` | 改密码（自己的不需 root）| 改自己否 | 它本身是 setuid 程序（第 04 章）|
| `groupadd ml` / `groupdel` | 建/删组 | **是** | |
| `chage -l alice` | 看/改密码有效期策略 | 看自己否 | 读的是 shadow 的后几列 |
| `su - bob` | 切换成另一个用户（要对方密码）| 否 | `-` 表示加载对方完整登录环境 |

> ⚠️ `usermod -aG` **漏掉 `a`** 是经典事故：`usermod -G docker alice` 会把 alice 的附加组**重置为只剩 docker**，悄悄踢出 `sudo`、`ml` 等组。批量改用户时尤其要小心。

---

## 🔗 实战要点
- 进一台新机器先敲 **`id`**：立刻知道"我是谁、在哪些组、有没有进 sudo/docker 组的资格"。
- 加组后没生效 → **退出重登**（或 `newgrp`），别怀疑命令没成功。用 `id` 复查。
- 查用户优先 `getent passwd 用户名` 而非 `cat /etc/passwd`——后者在接了 LDAP/AD 的环境里看不全。
- 看到一堆 `nologin` 系统账号是正常的；真要排查"哪个服务以哪个身份跑"，看 `ps -eo user,comm` 或 `systemctl status 服务`。

## 🧠 速记卡
- 机器认人**不靠 IP**（身份 vs 来源，两层解耦）；靠名单 `/etc/passwd` + 密码 `/etc/shadow` + 组 `/etc/group`。
- passwd 一行 7 段：**名 / x / UID / GID / 备注 / 家目录 / shell**；`nologin` shell = 禁登录。
- **UID = 身份证号**（非指针）；文件盖号码、`ls -l` 查表翻名字；删用户 → 孤儿文件露号码。
- UID 区间：**0=root / 1–999=系统服务账号(nologin) / 1000+=真人**；服务用专属低权账号 = 最小权限隔离。
- **主组（建文件用，1个）vs 附加组（访问资源，多个）**；`id` 一眼看全。
- `usermod -aG` 的 **`a` 千万别漏**；加组后**重登才生效**。

## ✅ 自检
1. 为什么说"换个网络登录，机器照样认得你"？这说明身份和什么解耦？
2. 文件上盖的是名字还是号码？用数字的两个好处是什么？删用户后 `ls -l` 露出光秃秃数字，说明了什么？
3. `/etc/passwd` 里一半账号的 shell 是 `nologin`，为什么要给程序也建"用户"？这对安全有什么意义？
4. 主组和附加组的区别是什么？你新建一个文件，它的属组由哪个决定？
5. `usermod -aG docker alice` 和 `usermod -G docker alice` 差一个字母，后果差在哪？
6. 刚把自己加进 `ml` 组，`id` 却看不到，为什么？怎么办？

## ➡️ 下一章
机器认得你、知道你属于哪个组了——那"某文件你到底能不能读/写"由什么决定？那 9 个字符怎么读、怎么算、贴到目录上为什么含义全变？下一章《权限 rwx》。
