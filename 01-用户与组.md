# 第 01 章 · 用户与组

> **一句话本质**：机器认人**不靠 IP**（IP 是"你从哪来"，身份是"你是谁"，两层解耦）；它靠一份本地名单 `/etc/passwd`，并且内部用**数字身份证号 UID**（不是名字、更不是指针）给你记账。

## 📍 这一章在哪
- 前情：第 00 章说"管人"是三样东西之首。
- 本章：钻进"机器怎么认识一个人、怎么把人分队"。
- 贯穿：UID/GID 是第 02 章权限"对号入座"的依据。

---

## 1. 机器认人不靠 IP——身份与来源解耦

登录一台机器其实是**两层、两件事**：

| | 回答 | 谁管 |
|---|---|---|
| **网络层** | 你**从哪来**（IP/路由/堡垒机）| 网络 |
| **系统层**（本章）| 你**是谁**（用户名 + 密钥）| 操作系统 |

> **证据**：同一个账号 `alice`，从家里宽带、公司 WiFi、手机热点登进去——IP 每次都不同，但机器每次都认你是 `alice`，进去都是同一个家目录、同一批文件。
> 若机器靠 IP 认人，换个网你就成陌生人了——显然不是。**机器的住户名单里，一个字节的 IP 都不存。**

---

## 2. 住户名单：`/etc/passwd`

机器认得谁，写在一个**纯文本文件** `/etc/passwd` 里，一行一个住户。真实一行长这样——**字段用冒号分隔、之间没有空格**：

```
alice:x:1001:1001:,,,:/home/alice:/bin/bash
```

拆成 7 个字段看（下面为对齐讲解才加了空格，实际无空格）：

```
alice : x : 1001 : 1001 : ,,, : /home/alice : /bin/bash
 ①      ②    ③     ④    ⑤        ⑥            ⑦
```

| 列 | 含义 | 备注 |
|---|---|---|
| ① 用户名 | `alice` | 给人看的名字 |
| ② 密码占位 | `x` | 真密码不在这（见下）|
| ③ **UID** | `1001` | **你的身份证号**（核心）|
| ④ **GID** | `1001` | 你主组的编号 |
| ⑤ 备注(GECOS) | `,,,` | 全名/电话等子字段，用逗号分隔，全空所以只剩逗号 |
| ⑥ **家目录** | `/home/alice` | 登录后落脚处 |
| ⑦ 默认 shell | `/bin/bash` | 给你哪个"壳" |

> **密码去哪了？** 真正的密码哈希在 `/etc/shadow`（只有 root 能读，更安全）；SSH 公钥在你家目录的 `~/.ssh/authorized_keys`。`/etc/passwd` 里的 `x` 只是占位。

---

## 3. UID = 身份证号（不是名字，更不是指针）

机器内部**不用 `alice` 这个名字**记账，用数字 `1001`。注意：

> **UID 不是指针、不指向任何物理位置。它就是一个"身份证号/学号"——纯标识符。**

**为什么用数字而不用名字？两个真实原因：**

1. **改名解耦（最关键）**：每个文件的档案（inode）里盖的是**号码 1001**，不是名字。哪天 `alice` 改名 `alex`，只改名单里一行，号码 1001 不变 → 全盘文件自动还是你的。若盖的是名字，改名要改全盘，漏一个就"不认你"。
2. **定长又快**：号码固定 4 字节；名字是变长字符串，占地、比较慢。

**于是有一张"翻译表"：**
```
磁盘上文件只盖号码：           属主 = 1001
                                  ↕  （需要时查表）
/etc/passwd 是「号码↔名字」表：  1001 ⟷ alice
```
- 机器**内部记账/判归属**：永远用号码（快、稳、不怕改名）。
- 给**你看**（`ls -l`）：临时拿号码去查表，翻成 `alice` 显示。

**孤儿文件**：若某文件属主是 `1005`、而 `1005` 用户被删（表里没这行了），`ls -l` 翻不出名字，就露出光秃秃的号码：
```
-rw-r--r--  1  1005  1005  2048  old_data.bin
```
> 这反过来证明：文件上盖的从来是**号码**，名字是临时查表翻的，表没了只剩号码。

---

## 4. 组（group）= 把人分队

只讲"单个住户"不够，多人共享要把人**分队**——这就是组（GID）。

| | 是什么 | 比喻 | 号码 |
|---|---|---|---|
| **用户 user** | 单个住户 | 你这个人 | UID |
| **组 group** | 一队住户 | 你的部门/班级 | GID |

> **为什么要组？** 权限常常是给**一队人**、而非一个人。
> 例：你们仨 `alice / bob / carol` 共享数据集 `/data/datasets`，想让仨人都能读、外人不能碰。不必逐人授权——建个组 `ml`，把仨人拉进去，对目录说一句"本组可读"即可。以后来新人，拉进 `ml` 组自动有权限。

- **一人可同时属多个组**（你既在 `ml`、又可能在 `docker`）——像你既是"三班的"又是"篮球队的"。
- 组的名单在 `/etc/group`。

---

## 🔗 实战要点
```bash
whoami          # 我是谁（用户名）            [只读]
id              # 我的 UID/GID + 我在哪些组   [只读，最有用]
groups          # 我属于哪些组               [只读]
cat /etc/passwd # 看全机住户名单（号码↔名字） [只读]
```
> 进一台新机器先敲 `id`：立刻知道"我是谁、在哪些组、有没有进 sudo/docker 组的资格"。

## 🧠 速记卡
- 机器认人**不靠 IP**（身份 vs 来源，两层解耦）；靠名单 `/etc/passwd`。
- 一行记 5 要点：**名字 / UID / GID / 家目录 / 默认 shell**；真密码在 `/etc/shadow`。
- **UID = 身份证号**（非指针）；文件盖号码、`ls -l` 查表翻名字；删用户 → 孤儿文件露号码。
- **组 = 把人分队**，一人可属多组；用于"给一队人开权限"（如共享数据集）。

## ✅ 自检
1. 为什么说"换个网络登录，机器照样认得你"？这说明身份和什么解耦？
2. 文件上盖的是名字还是号码？用数字的两个好处是什么？
3. `ls -l` 属主列显示一个光秃秃的数字，发生了什么事？
4. 三个人要共享一个数据集目录，为什么用"组"比"逐人授权"好？

## ➡️ 下一章
机器认得你、知道你属于哪个组了——那"某文件你到底能不能读/写"由什么决定？下一章《权限 rwx》。
